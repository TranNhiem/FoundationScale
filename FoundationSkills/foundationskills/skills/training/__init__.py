
"""Training skills: knowledge-pack loaders, recipe selection, resource estimation,
feasibility checks and method selection.

This package hosts the Task D1 building blocks (``knowledge``, ``recipes``,
``estimate``, ``feasibility``, ``method``) plus, from other tasks, the training
planner (``planner``, ``cpt``, ``posttrain``, ``skill``) and launch emitters.
The submodules are imported explicitly to keep import-time side effects at zero.
"""
