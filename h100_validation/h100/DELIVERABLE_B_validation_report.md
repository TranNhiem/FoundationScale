# FoundationScale H100 Validation Report

**Validation state:** **PHASE 3 COMPLETE (8/8 legs, job 37310); Phase 4 two-model execution MEASURED at single-node scope; the full claim set remains NOT COMPLETE** (two OPEN findings, one OPEN invocation-record finding, a Gemma provenance caveat, single-node scope).  
**Hardware scope:** one measured node with eight H100 GPUs.  
**Distributed scope:** single-node execution only.  
**Current Phase 3 state:** eight of eight required legs executed through the framework (job 37310).  
**Elision note:** estate identifiers are elided throughout as `<PARTITION>`, `<NODE>`, `<HOME>`, `<IMAGE>`. A build gate scans this file case-insensitively and refuses the build over a single hit; where an identifier was uncertain, it was elided.

## 1. Verdict

**GPU execution through FoundationScale's shipped launch path is now MEASURED on eight H100s, and the genuine eight-GPU Phase 3 run has occurred.** Phase 3 executed 8/8 legs through the framework with no abstentions (job 37310, Qwen3-4B-Instruct-2507, COMPLETED 00:06:02, resume status PROVED). The first end-to-end execution of the full submission topology — probe → production → resume → post-mortem — completed with every link COMPLETED 0:0 (jobs 37340→37341→37342→37343, qwen3-0.6b). A second model family executed the same argv shape on the same corpus: the Gemma-3-1b arm (job 37336) abstained on the rank-invariance leg, and that divergence is attributable to the model, because the identical code path produced exact rank agreement (spreads 0.0, restore_delta 0.0) on the Qwen control at the same 0.0005 threshold (job 37342). The Gemma checkpoint was a third-party mirror, not canonical weights; that caveat is explicit in §3.4 and weakens the attribution. Re-baselining this report against the Phase 4 measured record surfaced three control failures in the plane's own reporting (#187, #188, #189) — each fixed and re-measured (jobs 37344, 37345, 37346), plus one regression they composed into, measured on both sides and then closed by measurement: the #171 success-arm guard that #189 carried into the artifact exited 95 above the shared adjudication tail on every post-mortem invocation, killing the #187 arm again (#193; dead on 37347, `adjudicated=2 of 2 ... ok=2` on 37348) — plus a class defect (fixes orphaned from the build's execution and publish lists) whose orphan direction is now closed by measurement rather than by assumption: gate #191 has landed, is wired into both the stage list and the publish set, and reports RUN_NOT_SHIPPED 0 over the 54 files it partitions. No finding is OPEN, but two of the three most recent closures are **build-certified only**: #192 (the split resume tolerance) and #180 (the launch-provenance record) each landed as a build stage with static gates and executed controls green, and neither has yet run on a cluster job — the fix is real, the hardware observation is owed. What remains unestablished: multi-node execution, MoE coverage, canonical-weight Gemma equivalence, the mechanism of the Gemma cross-rank divergence, and any claim broader than two small models on one node.

## 2. Validation chain

The required chain is Environment → Model Loading → Dataset → Distributed Training → Checkpointing → Resume Training → Evaluation. Chain coverage at a glance: job 37340 discharges Environment; 37341 discharges Model Loading, Dataset, Distributed Training and Checkpointing; 37342 discharges Resume Training and Evaluation; 37343/37344 discharge the post-mortem reporting link (dead on 37347, restored on 37348 — #193); 37345 discharges Checkpointing adjudication over the full tree; 37346 discharges the failure path; 37347 records the attempted Gemma-tree re-walk that #193 reduced to a no-op, and 37348 the same submit one build stage later, which adjudicated 2 of 2. Job 37310 discharges all seven links for the Phase 3 baseline.

| Link | Status | Evidence cited | What would move it further |
|---|---|---|---|
| Environment | MEASURED | One visible partition, one node, and eight H100s were enumerated; partition limit seven days; singularity present, apptainer and enroot absent; scheduler commands present; image contents inspected in-container [M1; M2; M11; M14]. Discharged again by every Phase 4 link (37340–37346) launching in-container on the measured node. | Rerun the account-dependent checks under the authorized credential; that portion remains BLOCKED (§4.1). |
| Model Loading | MEASURED | Full BF16 loads through the framework launch path: Qwen3-4B (37310), qwen3-0.6b (37341), Gemma-3-1b via third-party mirror (37336, 37319). Nonzero materialized state on 8/8 ranks, with stateful optimizer denominators of 529 (37319) and 513 (37342) entries. Provenance caveat for the Gemma weights: §3.4. | Canonical `google/` Gemma weights (UNMEASURED, §3.4); MoE and additional architecture families remain UNMEASURED. |
| Dataset | MEASURED | The real `phase3_real_4k` corpus drove both arms, identical across models. Held-out evaluation scored 3709/3709 tokens (37342, eval loss 2.0253733) and 58001 held-out tokens (37319, eval loss 1.1863452). | One dataset is the denominator. A planted unreadable-input MUST_FIRE has not been observed (§5.2). |
| Distributed Training | MEASURED | Framework-launched eight-rank FSDP training: 37310 (5041.991 tokens/s segment 1; 3731.470 post-resume), 37319 (5517.637 / 4527.572), chain production 37341. The earlier manual NCCL baseline (28.0 vs reference 28.0, 420.8 GB/s, 256 MiB [M12]) is superseded as transport evidence of record by framework runs. | Multi-node execution remains BLOCKED: one node on the estate. |
| Checkpointing | MEASURED, with a corrected denominator | Real training checkpoints written and adjudicated. The shipped END-line figure `checkpoint_saves_adjudicated=1` on 37341, 37342 and 37344 is the #188 truncated denominator — numerator and denominator were cut by the same defect over a three-checkpoint tree, and each job still recorded COMPLETED 0:0. Corrected by 37345: `adjudicated=3 of 3 ... ok=3 abstain=0 refuse=0`, three `VERDICT 0 PASS` lines, ten measured checks each. The Gemma tree (37319: `ADJUDICATE complete ... checkpoint_dirs=1` over two checkpoint directories — a fourth #188 observation) was re-walked under the fix on the current artifact (37347) and returned nothing — the #171 guard exits 95 above the shared adjudication tail on the post-mortem arm, so `ADJUDICATE` occurred 0 times (#193) — and then re-walked again on 37348, the same submit one build stage later: COMPLETED 0:0 in 5m13s, `ADJUDICATE complete ... adjudicated=2 of 2 checkpoint dir(s) ok=2 abstain=0 refuse=0`, `END rc=0 phase=post-mortem checkpoint_saves_adjudicated=2 checkpoint_saves_found=2`, both checkpoints green on A1–A7b at `checks_measured=10 checks_green=10 checks_red=0 legs_abstained=0`, `world_size=8` with 8/8 rank shards, steps 5 and 20. The Gemma tree's checkpoint count is MEASURED. | Observe the new truncation guard fire (§5.2). |
| Resume Training | MEASURED | 37310: status PROVED, `maximum_rank_difference 0.0`. 37342: `restore_delta 0.0`, `cross_rank_spread_after_resume 0.0` on all 8 ranks, optimizer-state mismatches 0 of 513, payloads 8/8. The Gemma arm's rank-invariance leg abstained (37336: `unmeasured:["resume.fixed_eval_rank_invariance"]`) — an abstention, not a pass, and it stays one. | A deliberately corrupted real checkpoint resumed under the comparator (synthetic control C3 only so far, §5.2). |
| Evaluation | MEASURED | Held-out evaluation with denominators: 37342 eval loss 2.0253733 over 3709/3709 tokens; 37319 eval loss 1.1863452 over 58001 tokens; eval throughput 519.157 tokens/s (37310). | Labeled reference-answer accuracy remains UNMEASURED (§6). |

## 3. What was measured, and how

### 3.1 Executed measurements

| Measurement | Status | Result and denominator | Method and scope |
|---|---:|---|---|
| Estate inventory | MEASURED | Exactly one visible partition; one node; eight H100 devices; partition walltime seven days; queue empty at observation time [M1; M11]. | Scheduler inspection on the measured estate. Time- and credential-scoped. |
| Container runtimes | MEASURED | Singularity present: 1/1 required runtime on this estate. Apptainer absent: 1/2 optional-runtime probes. Enroot absent: 2/2 optional-runtime probes. Scheduler launch tools present: 2/2 [M2]. | Host command discovery plus scheduler inspection. The source-level enroot arm cannot be executed on this estate. |
| Container image contents | MEASURED | Five Megatron-family import probes reported unavailable: 5/5. The HF training stack components were present and importable: 7/7, plus distributed-checkpoint import [M14; PHASE3-SPEC]. | In-container import probes. The image is an HF/FSDP image, not a usable Megatron image. |
| Mount boundary | MEASURED | In a two-arm in-container test, the asset filesystem was absent without a bind and visible with a declared bind: 2/2 arms differentiated [M16]. | Controlled singularity execution differing only in the declared bind. Raw paths are replaced by `<asset-root>`. |
| Torch-leak control | MEASURED | With a synthetic host torch stub and containment unset, the container imported the stub: 1/1 MUST_FIRE. With containment enabled, container torch: 1/1 MUST_PASS [M10]. | Two-arm synthesized leak experiment. |
| Torch provenance detector | MEASURED | Four of four checks passing; always-accept sabotage exited nonzero: 1/1 MUST_FIRE [M7]. | Discrimination within the detector's measured prefix scope only; see M13. |
| Environment boundary | MEASURED | Ten required names crossed: 10/10. Four host-only names held: 4/4. Suffix-similar name did not leak: 1/1 [M6]. | Executed against the real spliced runtime files. |
| Bind-path population | MEASURED | Empty derivation refused: 1/1 MUST_FIRE. Four declared inputs reduced to three unique binds: 1/1 MUST_PASS. Bind gates B1–B8 green: 8/8 [M18]. | Executed patch gates. |
| Resume-name forwarding | MEASURED | Both required resume names crossed: 2/2 MUST_PASS. Three forbidden or malformed names refused: 3/3 MUST_FIRE. Harness extracted 23 names [M20]. | Forwarding only; trainer-side restore correctness is now measured separately (37310, 37342). |
| Bidirectional environment drift | MEASURED | Gate went red on two real directions; all three detectors red under planted violations: 3/3 MUST_FIRE. Remediation gates 6/6 green [M21]. | Executed against the real launcher/backend pair. |
| Estate-path policy | MEASURED | Sixteen estate-literal matches over four lines replaced by configured roots; emitted result 0 matches from a stated pre-patch denominator of 16 [M22]. | SOURCE-LEVEL scan plus execution of the generated matcher. |
| Launch-topology composition | MEASURED | Topology patch 9/9 green. Eleven composer rows executed: four MUST_PASS, seven MUST_FIRE. Eight-GPU/one-rank refused: 1/1 [#124; M23]. | The separate launcher blockers M3/#142 listed here at the time are now closed by measurement (§4.2). |
| Communication baseline | MEASURED | Manual eight-rank run: world size and device count both 8; collective 28.0/reference 28.0; 256 MiB reduction in 1.12 ms at 420.8 GB/s [M12]. | In-container `torchrun`, bypassing FoundationScale. Transport baseline only; superseded as evidence of record by framework runs. |
| NCCL plugin controls | MEASURED | Image-default plugin faulted once: 1/1. Network plugin disabled: PASS once: 1/1. Poisoned data and crashing rank both detected: 2/2 MUST_FIRE [E.3]. | Eight-rank communication probe. The chain runs set `FS_NCCL_NET_PLUGIN=none`. |
| Root resolver on estate | MEASURED | Eight of eight estate rows reproduced the resolver prediction: 8/8. Two ambiguous roots refused with candidate counts; two shallowest-depth controls resolved [E.2; #133]. | Resolver executed inside the declared container (host interpreter is Python 3.6.8). |
| Synthetic checkpoint adjudication | MEASURED | Four synthetic inputs produced four distinct outcomes: nonexistent→refuse 96; empty→UNMEASURED 95; junk-only→UNMEASURED 95; complete synthetic→A1–A7a green. Dropping one shard moved 8/8 green to 6/8 green [ADJ]. | Synthetic artifacts only. Real-tree adjudication is now measured: 37345, below. |
| **Phase 3 baseline, framework execution** | MEASURED | Job 37310 (Qwen3-4B-Instruct-2507): COMPLETED 00:06:02. All eight legs reported `verdict:"MEASURED"`, no leg abstained: 8/8. Resume status PROVED; `fixed_loss.after_resume 0.8793521523475647`; `maximum_rank_difference 0.0`; train throughput 5041.991 tokens/s (segment 1) and 3731.470 tokens/s (segment 2, post-resume); eval throughput 519.157 tokens/s; manifest `fixed_loss_before_save` present 1 of 1, `global_step = 20`. | Framework launcher, singularity, eight H100s, one node. |
| **Phase 4 Qwen chain** | MEASURED | Jobs 37340 (probe) → 37341 (production, `afterok:37340`) → 37342 (resume, `afterok:37341`) → 37343 (post-mortem, `afterany:37341`): every link COMPLETED 0:0. First end-to-end execution of the full topology. Model qwen3-0.6b; corpus `phase3_real_4k`, identical to the Gemma arm; singularity; `FS_GPUS_PER_NODE=8`; one node; `FS_NCCL_NET_PLUGIN=none`. Both training links carry `verdict:"MEASURED"` with empty `unmeasured` lists. Their END lines read `checkpoint_saves_adjudicated=1` — the #188 truncated denominator; the corrected figure over the same tree is 37345's 3 of 3. | `FS_SUBMIT_CHAIN=1` submission; dependency chain and exit codes read from accounting. |
| **Resume proof, job 37342** | MEASURED | `restore_delta 0.0`; `cross_rank_spread_after_resume 0.0` on all 8 ranks; optimizer-state mismatches 0 of 513; checkpoint payloads present 8/8; held-out eval loss 2.0253733; held-out tokens scored 3709/3709; peak GPU memory 3.535 GiB. | Trainer's own instrumentation, independent of the adjudicator; unaffected by #188. |
| **Phase 4 Gemma arm, job 37336** | MEASURED (abstention stands) | Config `phase4_gemma.json`; `--resume-tolerance 0.0005`, the same threshold the Qwen arm ran at. Rank-invariance leg abstained: `unmeasured:["resume.fixed_eval_rank_invariance"]`; `cross_rank_spread_before_save` and `cross_rank_spread_after_resume` both 0.2940967082977295; `restore_delta 0.0`. The job then died on the #174 self-kill path: 1163 log lines, `srun: error: task 0: Exited with exit code 1`, 0 END lines, 0 FATAL lines, sacct FAILED 0:15. The abstention survives only because the trainer wrote its summary before the launcher killed itself. | **Corrected attribution:** earlier text assigned this abstention to job 37319. The logs do not support that; see the #192 ledger row and §4.2. |
| **Gemma tolerance run, job 37319** | MEASURED | Config `phase3.json`, 200 iterations, `--resume-tolerance 10.0`: COMPLETED 0:0 in 6m12s, `unmeasured:[]` — zero abstentions. `maximum_rank_difference 0.17570888996124268` with before/after fixed loss bit-identical at 0.5986318588256836; 0 of 529 stateful optimizer entries mismatched; throughput 5517.637 / 4527.572 tokens/s; peak GPU 9.062 GiB; eval loss 1.1863452 over 58001 held-out tokens. Its `ADJUDICATE complete ... checkpoint_dirs=1` over a two-checkpoint tree is the fourth #188 observation; that tree's re-walk under the fix was attempted on the current artifact and produced 37347, which adjudicated nothing because of #193 — UNMEASURED, with a measured cause. At the shipped tolerance 0.0005 the identical run REFUSED (`fixed loss changed by 0.16057962`, `fixed_loss_before_save = 0.6053718328475952`). | Same checkpoint, corpus and node count as 37336; different config and threshold. Not interchangeable with 37336. |
| **Cross-rank forensics, jobs 37320–37323** | MEASURED | Zero training steps, 8/8 ranks, reusing the trainer's own `load_artifacts`/`build_dataset_context`/`build_runtime`/`save_checkpoint`/`_evaluate_fixed_loss`. Within-rank restore delta 0.0 (4/4 jobs); fingerprint keys changed 0 of 341; weights divergent 0 of 340 across 8/8 ranks; inputs identical 8/8; cross-rank loss spread 1.1025075912475586 at seqlen 1024 (37320–37322) and 0.7679 at seqlen 512 (37323). Ruled out, each with its denominator: data/row indexing; weights; restore fidelity; attention backend/model class/dtype; sequence length and the 512 sliding window (a stated prediction falsified); dropout; logit softcapping; missing checkpoint keys (341/341); wrap/load divergence. | The spread is bit-stable, takes exactly two values across the eight ranks, and is not sampling jitter: the same-runtime noise floor is 0.0. |
| **Checkpoint scalar instrument** | MEASURED | `fs_ckpt_scalars.py` recovers per-rank pre-save loss with zero GPU-seconds (stub unpickler; no torch). Certified in place on the login-node Python 3.6.8: 6/6 legs. Gemma step-50 vector, 8/8 ranks: spread 0.17570888996124268 — bit-for-bit the value 37319 reported as its resume statistic, so that statistic carried no restore information. Qwen controls: spread 0.0 at steps 5 and 50, 8/8 ranks each. Cross-check: `optimizer_state_count` sums to 529, the denominator 37319 printed independently. Leg F found the tool's own defect during certification (an unqualified agreement boolean over an 8-of-9 denominator); fixed: `identical_across_present_ranks`, missing payload named, exit 95. Certification harness: 11 legs green on the build host and on the login node. | Artifact-level proof over checkpoints already on disk. Two legacy checkpoints (Qwen step 20, Gemma step 200) correctly declare `0 of 8 ranks recorded this key`, exit 95. |
| **Resume-proof attribution (#177, landed)** | MEASURED | 8/8 static gates, 8/8 controls, byte-idempotent on re-run; wired into both the publish set and the build stage list. Control C1 is fed the real measured vector from 37319: the shipped conflated statistic `legacy_delta = 1.102507` exceeds tolerance 0.0005 → REFUSED, naming resume; the patched rule on the same evidence reports `restore_delta = 0.0` (restore exact), `rank_invariant = False`, `spread_before = 1.102507` → `unmeasured_cross_rank`. A genuinely lossy restore still goes RED (C3: `restore_delta = 0.004999999999999893`); RED outranks divergence (C4); legacy absence declared UNMEASURED (C5); malformed evidence refused (C6/C7); strict `>` boundary (C8); verdict function pure (G5b: 832 AST nodes, torch refs=0, dist refs=0). | One number had answered two questions; the fix separates the two verdicts. It does not separate the two thresholds — that is #192, OPEN. |
| **Post-mortem link fix (#187)** | MEASURED | Before (37343): one log line, no END line, no verdict line, 0 of 3 checkpoints adjudicated, exit 0. After (37344): COMPLETED 0:0 in 3m40s; the link falls through backend init, runtime setup, the bind plane, the in-container GPU census and `fs_compose_launch`, skips only the training launch, and reuses the existing adjudication tail and END line verbatim (`phase=post-mortem`). That after-state was true of the artifact 37344 ran on; it is false of the current artifact, where the same link reaches `ADJUDICATE` 0 times and exits 95 above the tail (37347), because #189 landed the #171 guard above it — #193. | Full before/fix/after in §5.3. |
| **Adjudicator denominator fix (#188)** | MEASURED | Before: 37341, 37342 and 37344 each COMPLETED 0:0 with `checkpoint_saves_adjudicated=1` over a tree of three checkpoint directories. After (37345): COMPLETED 0:0 in 5m51s — `ADJUDICATE complete ... adjudicated=3 of 3 checkpoint dir(s) ok=3 abstain=0 refuse=0`; three `VERDICT 0 PASS` lines, `checks_measured=10` each; the END line prints `checkpoint_saves_adjudicated=3 checkpoint_saves_found=3`. | Full before/fix/after in §5.3. |
| **Failure-path fix (#189)** | MEASURED | Before (37304, recorded): sacct CANCELLED 0:15, 0 END lines, 0 FATAL lines, exit never reached. After (37346, deliberate failure injected — one unrecognized trainer flag appended to the passing arm's argv): sacct FAILED 5:0; the END line is the last line: `END rc=1 mapped_rc=5 phase=train FAILED (fs_map_run_verdict: verdict=NONE rc=1 mapped=5 -- no verdict line; an undeclared death is a FAILURE, not an abstention)`. Positive control: the argparse error appears once per rank, 8/8. | Full before/fix/after in §5.3. |
| Current build | MEASURED | All 20 build stages completed green: 20/20. Generated suites passed 31/31 tests in 0.08 s. Seven generated artifacts parsed cleanly: 7/7. The latest record reports zero blocklist hits but does not state its file denominator [CUR-BUILD]. | Historical record; superseded by the post-re-baseline build below. |
| **Current build, post-re-baseline** | MEASURED | All six orphaned stages (#188, #189, and the five #190 stages) are wired into both the stage list and the publish set, and the #191 stage-orphan gate is wired into both as well; the build is green at 33 stages with the gate green inside it. | Build after wiring #191: 33 stages, drift gate green, 0 blocklist hits, 7/7 parse clean, naming agreement green, linkage green, launch-doc green, suite 31 passed in 0.10s, STAGE-ORPHAN GATE GREEN. The earlier one-off audit (two files in neither list, one superseded) is superseded as evidence by the gate's own measurement: 54 files partitioned, RUN_NOT_SHIPPED 0 (§4.3). |
| Current-build determinism | UNMEASURED | An earlier 17-stage build produced byte-identical hashes for five artifacts across two rebuilds: 5/5 at that historical state. No double-build hash comparison exists for the current 33-stage build [E.5; CUR-BUILD]. | Requires two consecutive complete current builds plus hashes for all artifacts. |

### 3.2 SOURCE-LEVEL findings

A SOURCE-LEVEL result is a read, search, parse, or generated-file inspection. It is not runtime evidence.

| Source-level result | Status | Evidence and denominator |
|---|---:|---|
| Original runtime concentration | MEASURED | The original framework source contained 58 enroot references and zero singularity references. After splicing, functions increased from ten to thirteen, no function was lost, all twelve distinct `fs_*` calls resolved, shell parsing clean [M2; M8]. |
| Original generated-file integrity | MEASURED | Post-splice reference inventory contained 46 singularity references; the higher enroot count did not remove that runtime's source-level arm [M8]. |
| Time-limit guard | FAILED | Two checked values were admitted, including values below and above the seven-day partition limit: 2/2 admissions. The final pattern matches any value containing a colon [M4]. Still FAILED; not re-measured in Phase 4. |
| Iteration-budget variables | FAILED | Two exported budget names had zero readers across three generated artifacts: 0/3 [M5]. Still FAILED as recorded; bounded runs have since executed (37310, 37319, 37341), but the variable-consumption defect itself has not been re-audited. |
| Torch-provenance prefix scope | FAILED | The container has eight legitimate site directories: 8/8 enumerated. The detector pins one of them: 1/8 covered by its assumed prefix [M13]. |
| Mount-declaration scope | FAILED | The R4 implementation verified readability of each declared path, not recursive materialization beneath it [E.4; #132]. |
| Checkpoint writer/adjudicator contract | CLOSED BY MEASUREMENT | Recorded FAILED: cross-validation abstained on 2/2 framework checkpoint formats [ADJ; #150]. Contradicted by job 37345: 3 of 3 real framework checkpoints adjudicated, `ok=3 abstain=0 refuse=0`, ten measured checks each. See §4.2. |
| Parser coverage | MEASURED | After repair, the backend had thirteen parsed definitions and the launcher six, with zero unparsed definitions; both suites reran green [M19]. |
| Missing build-produced launcher input | CLOSED BY MEASUREMENT | Recorded BLOCKED: the shipped launcher sourced a filename the build never produced [#142]. Contradicted by execution: jobs 37310, 37341 and 37342 ran the shipped launch path to COMPLETED 0:0, so the consumed file existed and was produced. See §4.2. |
| Adjudicator interface containment | CLOSED BY MEASUREMENT | Recorded BLOCKED: the required knob was neither containment-checked nor bound into the container [#146]. Contradicted by in-container adjudication on 37341, 37342 and 37344, and by the corrected full-tree walk on 37345. See §4.2. |
| Engine model-loading seam | MEASURED | The framework is written against a Megatron-Bridge-style seam while the estate image exposes HF transformers/accelerate/FSDP [M14]. The HF/FSDP seam has now executed end to end: 37310, 37341–37342, 37336, 37319. The Megatron engine remains absent from the image: 5/5 probes. |
| Toolchain-version overlap | UNMEASURED | Image probes and the Phase 3 specification report transformers 5.5.0; Deliverable E's architecture/meta matrix reports 4.57.1. The two matrices remain separate toolchain-scoped observations [M14; E.1; PHASE3-SPEC]. |

### 3.3 Model evidence boundary

| Claim | Status | Measured denominator |
|---|---:|---|
| Configuration acceptance, transformers 5.5.0 | MEASURED | Five of five checkpoints resolved through `AutoConfig`: 5/5 [M17]. |
| Architecture/meta instantiation matrix, transformers 4.57.1 | MEASURED | Four of seven candidates built, two refused as unrecognized, one UNMEASURED: 4/7, 2/7, 1/7 [E.1]. |
| Full-weight model loading | MEASURED | Three checkpoints across two families loaded full weights through the framework and trained: Qwen3-4B (37310), qwen3-0.6b (37341), Gemma-3-1b via mirror (37336, 37319): 3/3 executed, 8/8 ranks each. Against the Deliverable E inventory of seven, full loading remains 3/7. |
| Second-architecture full loading | MEASURED, with provenance caveat | Gemma-3-1b loaded and trained end to end (37336, 37319) — from the third-party mirror, not canonical weights: §3.4. |
| Canonical-weight Gemma equivalence | UNMEASURED | The canonical repository returned HTTP 401; the canonical weights were never fetched: §3.4. |
| Mixture-of-experts coverage | UNMEASURED | The executed models are dense: 0/2 exercised the MoE expert-FQN logic [M9]. |
| Out-of-tree Gemma-4 registration | BLOCKED | Two out-of-tree Gemma-4 candidates were refused by the measured Deliverable E toolchain until a declared architecture-registration seam exists: 2/2 [E.1; #119]. |
| Dense multimodal interpretation | MEASURED | Two green rows over the original dense-multimodal pair would certify only dense multimodal loading; they carry no MoE and no non-multimodal evidence [M9]. |

### 3.4 Gemma checkpoint provenance — explicit caveat

The Gemma checkpoint used in every Gemma measurement above (37336, 37319, and the forensics jobs 37320–37323) was the third-party mirror **`unsloth/gemma-3-1b-it`**. The canonical **`google/gemma-3-1b-it`** repository is gated and returned **HTTP 401** to this estate's credentials; the canonical weights were never fetched.

This is recorded as a caveat on attribution, not as a footnote, because it changes the strength of the campaign's central cross-model result. The cross-rank forward divergence (§3.1, forensics) is measured against the mirror artifact. As a characterization of *the artifact that ran*, the measurement stands on its own denominators. As a claim about *Gemma-3-1b-it*, it carries the weaker attribution that attaches to any third-party mirror: the bits that diverged were not verified to be the bits Google published. The Qwen-side control is unaffected (its provenance is not in question), and the framework-side conclusion — the identical code path produced exact rank agreement on the control at the same threshold — is unaffected. What is weakened is only the model-side naming.

**Current state: UNMEASURED for canonical-weight equivalence.** What would close it: re-run the Gemma arm (`phase4_gemma.json`, `--resume-tolerance 0.0005`, unchanged argv) against `google/gemma-3-1b-it` weights once access exists, and record whether the 0.2940967082977295 spread reproduces, with the same denominators.

## 4. Findings-ledger summary

The normalized report below contains **42 finding records**. The denominator is these 42 records, not every test case, gate row, or historical note. `MEASURED` in this section means that the finding and its closure or retraction were themselves observed; it does not mean the corresponding training capability is operational.

**Re-baseline audit.** Sections 4, 4.1 and the finding records were audited row by row against `PHASE4_FACTS.md` and `PHASE4_FACTS_QWEN_CHAIN.md`. **Rows audited: 29** (the whole ledger at audit time: 4 blocker rows plus 25 finding records). **Revised: 4** (M3, #142, #146, #150 — each contradicted by measurement; moved to §4.2, not deleted). **Unchanged: 25.** **Added during re-baseline: 12** (#169, #171, #174, #177, #179, #180, #187, #188, #189, #190, #191, #192); **added since: 1** (#193, the regression exposed when the Gemma-tree re-walk returned nothing). The ledger now stands at 42 records.

| Status | Count |
|---|---:|
| MEASURED | 29/42 |
| FAILED | 8/42 |
| BLOCKED | 2/42 |
| OPEN | 3/42 |
| UNMEASURED | 0/42 |
| **Total** | **42/42** |

### 4.1 Open blockers

| Blocker | Status | Full statement | Required closure evidence |
|---|---:|---|---|
| Requester-held credential | BLOCKED | Production push and credential-scoped reruns remain blocked until the authorized credential is supplied through the secure channel. Account-scoped facts — permissions, user-site contents, quotas, module visibility — are not transferable [CONFOUNDS; BLOCKERS]. Annotation: framework execution itself is no longer blocked by this row — jobs 37310 and 37340–37346 ran on the estate — but the Phase 4 record does not state under which credential, so the account-scope caveat stands unchanged. | Supply the credential, push the immutable artifacts, rerun credential-scoped environment and torch-provenance controls with unchanged artifact hashes. |
| ~~#193 post-mortem adjudication arm dead on the current artifact~~ | CLOSED | Regression of #187, measured on both sides. On the current artifact (37347, post-mortem `afterany` link against the Gemma tree `out_probe`, two checkpoint directories on disk): `ADJUDICATE` occurs 0 times, the run exits 95, sacct FAILED 95:0 — because the #171 success-arm guard that #189 carried into the artifact sits above the shared adjudication tail and refuses `rc=0` when the log carries no trainer verdict line, which on the post-mortem arm is every invocation by construction. On the pre-#189 artifact (37344, same link, same shape): the tail was reached, `ADJUDICATE complete ... phase=post-mortem`, sacct COMPLETED 0:0. **Closed**: the scoped fix landed as build stage 34 with all five controls fired, and job 37348 — the 37347 submit re-run verbatim — returned `adjudicated=2 of 2 ... ok=2 abstain=0 refuse=0`, `checkpoint_saves_adjudicated=2 checkpoint_saves_found=2`, COMPLETED 0:0. The Gemma tree's checkpoint count is now MEASURED. | Landed: the scoped fix — the #171 mapper applies only when `FS_SKIP_TRAIN != 1`; the else arm records that the trainer-verdict check was deliberately not applied and names the adjudication tail as this arm's verdict source — with its five controls observed: MUST_FIRE/DEAD_BRANCH, MUST_PASS/REACHES_TAIL, MUST_FIRE/EMPTY_TREE_STILL_95, MUST_FIRE/PARTIAL_COVERAGE_STILL_5, MUST_PASS/TRAIN_ARM_UNCHANGED — all five observed at build time on fragments lifted from the real pre- and post-image, and the Gemma re-walk executed as 37348. |

### 4.2 Closed by measurement (re-baseline revisions)

These four rows described as open, unmeasured, or blocking something the Phase 4 runs settled. They are kept here, with their original claims, because a row that turned out to be wrong is itself a result.

| Row | What the row claimed | Contradicting measurement | Correct present status |
|---|---|---|---|
| M3 — host-side GPU probe | BLOCKED. The generated launcher's GPU probe used the host interpreter (Python 3.6.8, no torch), received an empty visible-device count and refused with exit 96 before container initialization; no GPU could run under the shipped launch path. | Job 37310 (Phase 3, 8/8 legs MEASURED through the framework launcher, COMPLETED 00:06:02) and jobs 37340–37342 (probe, production, resume, each COMPLETED 0:0) executed the full launch path on eight GPUs. | CLOSED BY MEASUREMENT. The launcher launches eight in-container ranks; the host-interpreter probe is no longer on the executed path. |
| #142 — missing launcher input | BLOCKED. The shipped launcher sourced a filename the build never produced; a fix was generated but not landed, so no producer/consumer proof existed. | Jobs 37310, 37341 and 37342 ran the shipped launch path to COMPLETED 0:0; the consumed filename existed at execution time. | CLOSED BY MEASUREMENT. Producer/consumer is proven by execution. |
| #146 — adjudicator knob not contained | BLOCKED. The required checkpoint-adjudication knob was neither containment-checked nor bound into the container; its in-container visibility had no produced evidence. | In-container adjudication executed on jobs 37341, 37342 and 37344, and the corrected full-tree walk on 37345 adjudicated 3 of 3 checkpoints with ten measured checks each. | CLOSED BY MEASUREMENT. The knob is visible and consumed in-container. (The shipped denominator it printed was truncated — that is #188, a separate finding.) |
| #150 — checkpoint-format disagreement | FAILED. The writer emitted one timestamp-style directory format while the adjudicator accepted neither of the two produced formats: cross-validation abstained on 2/2 framework checkpoint formats. | Job 37345: `adjudicated=3 of 3 ... ok=3 abstain=0 refuse=0` over real framework checkpoints (`checkpoint-step-00000002`, `-00000005`, `-00000020`), three `VERDICT 0 PASS` lines, `checks_measured=10` each. | CLOSED BY MEASUREMENT. Writer and adjudicator agree on the produced format; zero abstentions over the real tree. |

### 4.3 Remaining finding records

| Finding | Status | Measured disposition |
|---|---:|---|
| #107 host-torch leak | MEASURED | Reproduced by synthesis in one arm and suppressed by containment in the second: 2/2 controlled arms discriminated [M10]. |
| #115 environment forwarding | MEASURED | Ten required variables crossed: 10/10; four host-only variables held: 4/4 [M6]. |
| Provenance detector control | MEASURED | Four of four checks passed; always-accept sabotage went red: 1/1 [M7]. Its narrowed prefix scope remains a separate FAILED finding, M13. |
| #117 mount plane | MEASURED | Bind gates 8/8 green. Empty derivation refused once: 1/1. Four inputs produced three unique binds once: 1/1 [M18]. |
| #122 resume environment | MEASURED | Both resume names cross: 2/2; three negative-name controls refused: 3/3; extraction denominator 23 names [M20]. |
| #116 drift gate | MEASURED | Both forward and reverse defects went red before remediation; all three detectors fired under planted violations: 3/3 [M21]. |
| #123 estate literals | MEASURED | Sixteen blocklisted literals over four lines removed and replaced by configured roots: 16→0 [M22]. |
| #124 rank topology | MEASURED | Nine topology gates passed: 9/9; eleven composer controls discriminated, including refusal of the eight-GPU/one-rank configuration: 11/11 [M23]. |
| #133 model-root resolver | MEASURED | Twelve generated tests passed: 12/12; eight estate predictions reproduced: 8/8 [E.2]. |
| #129 NCCL plugin | MEASURED | Default configuration faulted once; selected remediation passed once. Both negative communication controls observed: 2/2 [E.3]. |
| #136 generated intermediates | MEASURED | The missing intermediate stage was incorporated and the ungenerated intermediate is removed/rebuilt each run [E.5]. First recorded instance of the orphan-fix class; see #188, #189, #190 and §5.3. |
| #137 build-input partition | MEASURED | Three non-artifact inputs relocated and documented with hashes. Moving one away made the build red and restoring it made it green: 1/1 control [E.5; #137]. The gate also went RED at `10 present, 8 declared` when the hand-written instrument files were dropped into the generated directory; moving them to the repository root is 4/4 green with no change to the contract. |
| #138 host-interpreter assumption | MEASURED | The host has Python 3.6.8. The estate gate executed in the declared container and reproduced 8/8 rows [E.2; #138]. The checkpoint scalar instrument is certified in place on that interpreter: 6/6 legs. |
| #86 orphan suite | MEASURED | The build executes the generated suite; a missing suite is a declared build failure unless the explicit waiver is used [E.5; CUR-BUILD]. |
| M19 parser blind spot | MEASURED | Previously unparsed single-line functions are now counted; backend and launcher parsing ended with zero unparsed definitions [M19]. |
| **#174 launcher self-kill** | MEASURED | Recorded fixed; it was not. Reopened by #189's measurement: the failure path handed the hard-stop helper `"$$"` — the launcher's own pid — with 0 traps installed, so the shell died from SIGTERM and the END line, the exit, and the helper's container-cleanup arm were all unreachable. Observed in the wild on 37336 (FAILED 0:15, 0 END lines). Fixed by the fs175 stage; verified by job 37346: the launcher survived its own failure path and reached its own exit. |
| **#169 undeclared-death contract** | MEASURED | Recorded fixed; it was not. Reopened by #189. Fixed by fs175; verified by 37346: the mapper refuses to read a missing verdict as an abstention — `verdict=NONE rc=1 mapped=5`, 5 RED, not 95 UNMEASURED, because nothing declared itself. |
| **#171 exit-code flattening** | MEASURED | Recorded fixed; it was not. Reopened by #189. Fixed by fs175; verified by 37346: raw srun rc and mapped code are both recorded (`rc=1 mapped_rc=5`); torchrun had flattened the trainer's argparse rc 2 to 1, and the plane no longer publishes that flattened number as if it meant something. |
| **#177 resume-proof attribution** | MEASURED | Landed: 8/8 static gates, 8/8 controls, byte-idempotent, wired into both the publish set and the stage list. Control C1, fed the real 37319 vector, shows both halves at once: shipped statistic REFUSED naming resume; patched rule reports restore exact and cross-rank divergence `unmeasured_cross_rank`. Lossy restore still RED (C3); RED outranks divergence (C4). Separates the two verdicts; does not separate the two thresholds — that is #192, OPEN. |
| **#179 instrument self-certification** | MEASURED | `fs_ckpt_scalars.py` certified 6/6 legs on the login-node Python 3.6.8; harness 11 legs green on build host and login node. Two repairs before trust: a fixture defect caught by the harness failing closed at 3 of 11, and a control (C0) whose evidence was narrower than its claim, now split so each of the two unpickle obstacles has its own named piece of evidence. Leg F caught the tool printing an agreement boolean wider than its denominator; incomplete population is now a declared state with exit 95. |
| **#180 a run cannot reproduce itself** | CLOSED (build-certified; not yet exercised on hardware) | The entire trainer command line arrives through one knob and leaves no trace: zero echo/printf/tee sites for the raw or composed command between reading `FS_ENGINE_LAUNCH_CMD` and executing it. On 37319's own 936-line log, `FS_ENGINE_LAUNCH_CMD`, `--resume-tolerance`, `--model-path`, `--dataset-path` and `--sequence-length` occur 0 times combined. `tolerance = 10.0` is known only because the resume proof echoes it — an accidental witness, not a record. Reconstructing this campaign's own four-hour-old invocation took roughly eight tool calls and failed. **Closed by build stage 36, `patch_launch_provenance.py`:** immediately before the sole trainer exec, and only on the arm that actually execs one, the launcher writes `${RUN_LOG%.log}.provenance.json` — raw and composed command, world size and its source, the four directory knobs, phase, probe, job id, node count, and every `FS_` name the shell held. Certified `10/10 static gates, 8/8 controls`. Three design decisions are load-bearing and each was drilled: (1) the record's path is DERIVED from `RUN_LOG` rather than recomputed, so the log and its record cannot name different runs (the #150 lesson); (2) the census oracle is `compgen -v FS_`, not `compgen -e FS_` — the exported-only form silently omitted the launcher's two bare resume knobs, `FS_ITERATION_BUDGET` and `FS_EARLY_SAVE_STEPS`, which are exactly the two names that define a resume segment, so export state is now recorded per name in `fs_env_not_exported` instead of being used as a filter, and control C8 drilled red on the pre-image with `FS_BARE_KNOB present: False`; (3) a write failure announces `write=FAILED` and lets the launch proceed, because a run that cannot describe itself is degraded, not invalid. **Residual:** no cluster job has yet written one. |
| **#187 post-mortem link reported success over an empty denominator** | MEASURED | Before (37343): the `afterany` link's entire log was one line; it emitted no END line and no verdict line, adjudicated 0 of 3 real checkpoints over a denominator that was never printed, and exited 0 — sacct records COMPLETED 0:0, indistinguishable from a clean sweep. The link exists precisely for the case where production died, and in that case it reported success having measured nothing. Fix (fs187): the link no longer exits early; it falls through backend init, runtime setup, the bind plane, the GPU census and compose, skips only the training launch, and reuses the existing adjudication tail and its guards verbatim. After (37344, the artifact as it stood before #189 landed): COMPLETED 0:0 in 3m40s, END line printed with `phase=post-mortem`. That after-state is false of the current artifact: 37347 reaches `ADJUDICATE` 0 times and exits 95 above the tail — the fix is regressed by the #171 guard that #189 landed (#193). §5.3. |
| **#188 adjudicator walked 1 of 3 and said "complete"** | MEASURED | The shipped `adjudicate_tree` loop body drained the process substitution the loop read from, so it adjudicated the FIRST checkpoint only, then printed its count from the truncated stream it was measuring — numerator and denominator cut by the same defect, internally consistent at one third of the truth. Observed on three jobs, each COMPLETED 0:0 over a three-checkpoint tree: 37341, 37342, 37344 (`checkpoint_saves_adjudicated=1`); a fourth observation is 37319's `checkpoint_dirs=1` over a two-checkpoint tree. The fix had been authored under #175, verified and committed — and wired into neither the stage list nor the publish set (the class defect, below). Installed fix: collect into an array first, iterate second, redirect the adjudicator's stdin from `/dev/null`, count `found` and `processed` independently, refuse 96 if they disagree. After (37345): `adjudicated=3 of 3 ... ok=3 abstain=0 refuse=0`, three `VERDICT 0 PASS` lines, ten measured checks each. §5.3. |
| **#189 the failure path, measured with a deliberate failure** | MEASURED | Measured on the current build before any change: the marker `fs175:` occurred 0 times in both generated artifacts, the launcher carried 0 traps, and the failure path handed the hard-stop helper `"$$"`. Reopens #174, #169 and #171 — all recorded fixed, none in the artifact; `patch_launcher_exit_discipline.py` (676 lines) was in neither the stage list nor the publish set. Verified by causing a real failure (37346, one unrecognized flag on the passing arm's argv): sacct FAILED 5:0 (before: CANCELLED 0:15, which an orchestrator reads as a human cancel); the END line is the last line; the injected error reached 8/8 ranks. §5.3. |
| **#190 five stages ran but were never published** | MEASURED | Five build stages RAN but were absent from the publish set, so the published build script referenced five files a clone would not have: `patch_fabric_tripwire.py`, `patch_fsdp_wrap_policy.py`, `patch_master_addr.py`, `patch_nv_runtime.py`, `patch_train_phase_balance.py` — all real BLOCKER fixes. The existing coverage gate ran one direction only. All six orphans (these five plus #188's) are now wired in; the build was previously green at 33 stages, and is green at 36 today. |
| **#191 stage/publish-set coverage gate** | MEASURED | Landed and wired into both `build_h100_plane.sh` STAGES and `h100/PUBLISH_SET.txt`: `gate_stage_orphans.py` (26,690 bytes), with its declaration file `h100/STAGE_ROLES.tsv` (22 declaration rows) shipped alongside, because a clone that receives the gate without its contract gets a 95 on the first build — honest but useless. Measured on that green run — the figures below are the record of that measurement and are deliberately not restated as the plane grows; the live stage count is derived on every build by `gate_doc_stage_count.py` — RUN 33 stage entries (32 unique; `patch_list_separators.py` is deliberately invoked twice); SHIP 50 `.py`; ON_DISK 129; CANDIDATE 47 (45 by prefix family `patch_ apply_ extract_ gate_ emit_`, 43 named literally in the build script); 54 files partitioned — RUN_AND_SHIPPED 32, **RUN_NOT_SHIPPED 0** (the direction that produced #189 and #190, now empty by measurement rather than by assumption), SHIPPED_NOT_RUN 18 (build-driver 2, gate 8, library 1, runtime-artifact 4, test 3), NEITHER 4 (developer-tool 2, superseded 2). Exit-code contract 0 PASS / 5 RED / 95 UNMEASURED / 96 REFUSE; the gate's first run exited 95 `STAGE ROLES UNREADABLE` because `STAGE_ROLES.tsv` did not yet exist — fail-closed observed on the real artifact, not simulated. The `--propose` mode guessed five roles wrong (it labelled the trainer, the model-root resolver, the adjudicator and two pytest modules `gate`, because their basenames occur as literals in the build script), so every declaration row was hand-authored and a `build-driver` role was added so the two directly-invoked producers are not falsely declared gates — a role column is a machine-readable claim, and putting `gate` in it for a file that is not one is the same defect class the plane exists to refuse. This closes the orphan direction in both senses (a stage that runs and is not shipped; a file that ships and is never run) for the 54 files it partitions. It does not make the 18 SHIPPED_NOT_RUN files measured — 4 of them are declared `runtime-artifact`, an attestation that they execute on the cluster rather than in the build, and the declaration is not a measurement of that. |
| **#192 one scalar decides two different questions** | CLOSED (build-certified; not yet exercised on hardware) | `_resume_continuity_verdict` takes a single `tolerance` and uses it both for `restore_delta > tolerance → RED` and for `spread <= tolerance → the ranks agree`. #177 separated the two verdicts; it did not separate the two thresholds. The only way to stop a model's forward nondeterminism from abstaining is therefore to raise the number that also guards restore fidelity — demonstrated by the two Gemma runs: the same divergence abstaining at 0.0005 (37336) and passing at 10.0 (37319). At 10.0 the RED requires a delta of 10.0 in a quantity whose observed value is 0.0 and whose scale is a loss of 0.5986 — roughly seventeen times the loss itself — while the run still prints `why_tolerance: "continuity is therefore bounded, not claimed bit-for-bit"`. **Fix named:** two knobs, and the cross-rank one self-calibrating — compare the after-resume spread against the before-save spread, which the trainer already computes and already calls "the measured noise floor of the instrument". This is a framework-abstraction question, not a Gemma workaround: a self-calibrating floor generalizes across models, kernels and hardware, while an operator-supplied scalar must be re-tuned per model and silently couples restore fidelity to forward nondeterminism on every future model. The threshold question is posed by the framework's own verdict contract, so it must be answered at framework level. **Landed as build stage 35, `patch_resume_tolerance_split.py`, wired into both membership lists per #191.** `--resume-tolerance` keeps its name and now governs restore fidelity *only*: `restore_delta > tolerance` is RED and final, and no value of the cross-rank knob can reach it. A new optional `--rank-agreement-tolerance` (flag only, no env fallback, matching its neighbour) carries the cross-rank question. **The fix as landed differs from the fix as named above, and the difference is the point:** the row said the cross-rank knob would be *self-calibrating*, and a self-calibrating floor would have minted a pass from a threshold derived from the very run it judges — the same shape as a denominator derived from the stream it measures (#192's own class). As implemented, self-calibration REPORTS and never CERTIFIES. Left unset, the verdict records `cross_rank_spread_before_save`, `cross_rank_spread_after_resume`, their signed `cross_rank_spread_delta`, `rank_agreement_preserved` and `rank_agreement_tolerance_source: "self-calibrated"`, and declares the absolute claim `unmeasured_cross_rank`; `rank_invariant` may go True only under an EXPLICIT operator-supplied tolerance. Certified 10/10 static gates and 8/8 controls, byte-idempotent on re-application, `ast.parse` clean, with the controls executing the real patched function rather than a re-implementation of its rule. The decisive drill is `MUST_FIRE/RESTORE_RED_SURVIVES_WIDE_RANK_KNOB`: pre `[1.0]x8`, post `[1.5]x8`, restore tolerance 0.0005, rank tolerance 1000.0 -> `status=red restore_delta=0.5`, where the PRE-image function on the identical vector reads `status=pass`. That pair of observations is the regression pinned. `MUST_FIRE/SELF_CALIBRATION_NEVER_MINTS_A_PASS` (spread 7.0 on both sides, restore delta 0.0, no rank knob) reads `unmeasured_cross_rank rank_invariant=False` despite agreement being "preserved". The real 37336 vector reads `unmeasured_cross_rank rank_invariant=False cross_rank_spread_delta=0.0 source=self-calibrated` — `0.0` being the resume-attributable term the Gemma arm previously had no field in which to state. **Scope of the claim: BUILD TIME ONLY.** No cluster job has run the split-tolerance trainer, so the Gemma abstention has not been re-observed on hardware with the new fields populated. This is one certified fix, not one measured run. |
| **#193 post-mortem arm dead on the current artifact (regression of #187)** | MEASURED, CLOSED | Measured on three points of one arm. After (current artifact, 37347, Gemma tree `out_probe`, two checkpoint directories on disk): 8/8 srun steps COMPLETED, collective probe PASS, stderr 0 lines, launch log 51 lines, `ADJUDICATE` occurs 0 times; last line `END rc=0 mapped_rc=95 phase=train FAILED (fs_map_run_verdict: verdict=NONE rc=0 mapped=95 -- no verdict line; ...)`; sacct FAILED 95:0 in 2m36s — and the END line names `phase=train` on a run whose `FS_PHASE` is `post-mortem`. Before (37344, same link, same shape, artifact after #187 landed and before #189 landed): `fs175` occurs 0 times, 71 lines, `ADJUDICATE complete root=<HOME>/fs-phase4-qwen/out phase=post-mortem checkpoint_dirs=1`, `END rc=0 phase=post-mortem checkpoint_saves_adjudicated=1`, sacct COMPLETED 0:0 in 3m40s. One stage separates the two jobs. Cause: #187 routes the post-mortem link through the shared adjudication tail (`FS_SKIP_TRAIN=1`, `rc=0`); the #171 success-arm guard — refuse `rc=0` when the log carries no trainer verdict line — sits above that tail and exits 95 before it, and on the post-mortem arm no trainer ran, so there is no verdict line by construction: the guard's precondition is violated on every invocation and the tail below it is dead code, while the banner the operator sees still says the tree is adjudicated below. The #171 guard reached the artifact only when #189 landed, because the stage carrying it (`patch_launcher_exit_discipline.py`) was the #189 orphan: two individually correct fixes compose to a dead branch. No gate caught it because 95 is the correct code for a run that measured nothing — exit code, sacct state and log all agree that nothing was adjudicated — and #189's controls drilled one of the launcher's three arms (`train`, `resume`, `post-mortem`). **Rule: a fix that inserts a guard on a shared path owes a drill on every arm that path serves.** Fix in flight: scope the mapper to `FS_SKIP_TRAIN != 1`; on the else arm, record that the trainer-verdict check was deliberately not applied and name the adjudication tail as this arm's verdict source; the tail's own refusals stand unchanged (`checkpoint_observed -gt 0 || fail 95`; fs176 `observed -eq found || fail 5`). Controls: MUST_FIRE/DEAD_BRANCH (pre-image reproduces 37347), MUST_PASS/REACHES_TAIL, MUST_FIRE/EMPTY_TREE_STILL_95 (the load-bearing one — it stops the fix from reopening #187), MUST_FIRE/PARTIAL_COVERAGE_STILL_5, MUST_PASS/TRAIN_ARM_UNCHANGED. **Landed as build stage 34** (`patch_postmortem_verdict_scope.py`, wired into both `STAGES` and `PUBLISH_SET.txt`, which the #191 gate now enforces); all five controls fired at build time against real bash over fragments lifted verbatim from the pre- and post-image. **Closed by job 37348**, the 37347 submit re-run verbatim on the checksum-verified launcher: COMPLETED 0:0 in 5m13s, 91 lines, `ADJUDICATE complete root=<HOME>/fs-phase4-gemma/out_probe phase=post-mortem adjudicated=2 of 2 checkpoint dir(s) ok=2 abstain=0 refuse=0`, `END rc=0 phase=post-mortem checkpoint_saves_adjudicated=2 checkpoint_saves_found=2`, and a logged line recording that the trainer-verdict mapper was deliberately not applied on this arm. Both Gemma checkpoints green on all of A1-A7b (`checks_measured=10 checks_green=10 checks_red=0 legs_abstained=0`, `world_size=8`, `present 8/8 expected ranks`, `unexpected=[]`, `regular non-empty shards 8/8`, directory step agreeing with `manifest global_step` at 5 and at 20). |
| **#194 shipped documents state a countable the build derives** | MEASURED, CLOSED for the count | Four published documents carried three answers to one question: LAUNCH.md `17/17`, Deliverable B previously `33 stages`, Deliverable E `17/17`, and the build itself `36`. Nothing was wrong with the build — the number was TYPED in the documents and DERIVED in the script, so each new stage falsified four sentences at once and every document stayed internally consistent. Found while documenting #180. The same audit found the operator-facing lead box of LAUNCH.md still saying Phase 3 **"has never been executed"** — a full campaign after job 37310 executed 8/8 legs, the four-job chain completed (37340–37343) and a second family ran (37336). **Understatement is the same defect as overstatement:** the sentence was unreliable, and a reader who catches one stops trusting the rest. Fix: `gate_doc_stage_count.py` derives the count from the build's own `STAGES` array and refuses disagreement, over a printed denominator of 16 claims across 5 documents, with 5 controls including a planted-stale MUST_FIRE and a paired drill on the past-tense escape hatch (marked admits, unmarked refuses). It also surfaced a second ambiguity in the build's own output: `BUILD GREEN` counts 36 stage ENTRIES while `gate_stage_orphans.py` counted 35 unique FILES under the same word `stage(s)`. Both lines now name which they mean, and a document claiming 35 is drilled red. **Residual, stated not closed:** the narrative status lines were corrected by hand against the job record and are not machine-checked. Only the count is gated. |
| M4 time-limit guard | FAILED | Two tested limits, below and above the measured seven-day maximum, both admitted: 2/2 [M4]. |
| M5 iteration budget | FAILED | Two budget variables had zero consumers across three artifacts: 0/3 [M5]. |
| M13 provenance prefix | FAILED | The detector pins one of eight legitimate container site directories: 1/8 [M13]. |
| #132 mount-claim narrowing | FAILED | Verification established readability of declared roots only, not content beneath them [E.4; #132]. |
| #119 architecture-registration seam | BLOCKED | Two out-of-tree checkpoints refused by the measured matrix; require a declared extension seam: 2/2 [E.1]. |
| #127 launch-document generator | FAILED | The generator had never produced the launch document and was observed red on its L2 gate while correctly refusing to write a false artifact [E.4; #127]. |
| Retracted matrix row | FAILED | A row claiming three configuration candidates could not be reproduced; the row was removed rather than corrected [E.1]. |
| M9 dtype prediction | FAILED | The SOURCE-LEVEL prediction that a missing dtype field necessarily produced a crash or silent FP32 load was refuted by execution under transformers 5.5.0 [M15]. |
| M15 Gemma-family generalization | FAILED | A failure from one non-stock Gemma checkpoint was overgeneralized to the family/toolchain; stock Gemma configurations load, and the claim was retracted [M15; M17]. |

## 5. Controls: how the detectors are known to work

A detector without an observed MUST_FIRE is itself UNMEASURED. A green gate without a failure control is not evidence.

### 5.1 Detectors with an observed MUST_FIRE

| Detector or gate | Status | Observed MUST_FIRE | Observed MUST_PASS | Scope limit |
|---|---:|---|---|---|
| Torch provenance | MEASURED | Always-accept sabotage exited nonzero: 1/1; synthetic host torch imported by the leaked arm: 1/1 [M7; M10]. | Container torch imported with the same leak still planted: 1/1 [M10]. | Prefix premise is only 1/8 site directories; M13 remains FAILED. |
| Environment allowlist | MEASURED | Suffix-similar resume name refused: 1/1; three forbidden resume names refused: 3/3 [M6; M20]. | Ten required variables crossed: 10/10; both resume names crossed: 2/2 [M6; M20]. | Tests forwarding, not trainer behavior. |
| Bidirectional drift gate | MEASURED | Real forward and reverse defects went red before remediation; all three planted-detector drills fired: 3/3 [M21]. | Green after remediation; retained as a standing build gate [M21; CUR-BUILD]. | Both files must be regenerated and rechecked under the final build. |
| Bind-path population | MEASURED | Empty derivation refused: 1/1; B7 requires both runtime arms to consume the shared array [M18]. | Four declared inputs collapsed to three unique binds: 1/1 [M18]. | Declared-path readability is narrower than recursive materialization [#132]. |
| Estate-root matcher | MEASURED | Refused paths outside every root and a sibling-prefix boundary case [M22]. | A path under the second allowed root resolved: 1/1 [M22]. | Root list must be supplied at launch; fail-closed by design when omitted. |
| Launch topology composer | MEASURED | Seven of eleven clean-environment rows were negative controls; all seven refused. Eight-GPU/one-rank refused: 1/1 [M23]. | Four valid composition rows passed: 4/4 [M23]. | Composer controls are clean-environment; framework training ranks have now launched (37310, 37341). |
| Public blocklist | MEASURED | A planted token-shaped string matched once from one planted insertion: 1/1 [E.5]. | The current build reports zero hits, but its latest file denominator is unstated [CUR-BUILD]. | The live pattern control is historical; rerun the planted control against all current artifacts. |
| Build-input partition | MEASURED | A planted undeclared file is rejected on every build; went RED at `10 present, 8 declared` when hand-written files were dropped into the generated directory [E.5]. | Four acknowledged input/output categories accepted: 4/4 [E.5]. | Partitions declared files; does not infer hidden reads. The other direction is now gated: #191 has landed and measures RUN_NOT_SHIPPED 0 over the 54 files it partitions (next row). |
| Stage/publish-set orphan gate (#191) | MEASURED | 4/4 drills fired; the gate's first run on the real artifact exited 95 `STAGE ROLES UNREADABLE` because `STAGE_ROLES.tsv` did not yet exist — fail-closed observed, not simulated. | MUST_PASS output identical to the unperturbed run; the green run partitions 54 files with RUN_NOT_SHIPPED 0. | Closes the orphan direction for the 54 files it partitions. The 18 SHIPPED_NOT_RUN files include 4 declared `runtime-artifact` — an attestation of cluster-side execution, not a measurement of it. |
| Model-root resolver | MEASURED | Two real ambiguous estate roots refused: 2/2, candidate counts reported [E.2]. | Two shallowest-depth controls resolved: 2/2; eight estate predictions reproduced: 8/8 [E.2]. | Configuration search only. |
| Communication detector | MEASURED | Poisoned rank contribution and crashing rank both went red: 2/2 [E.3]. | Unpoisoned collective passed once: 1/1 [E.3]. | Manual transport probe. |
| Checkpoint adjudicator, synthetic set | MEASURED | Nonexistent, empty, junk-only, and drop-one-shard cases refused or removed green legs; the mutation moved 8/8 green to 6/8 green [ADJ]. | One complete synthetic checkpoint made every A1–A7a leg green [ADJ]. | Synthetic inputs; real-tree adjudication is the next row. |
| Checkpoint adjudicator, real tree | MEASURED | The shipped walker reported `adjudicated=1` over a three-checkpoint tree on three jobs (37341, 37342, 37344) — the defect is itself the observed failure of the old denominator, and the corrected walker's refuse-96-on-disagree guard now makes that shape a refusal. | Corrected walker: `adjudicated=3 of 3 ... ok=3 abstain=0 refuse=0`, three verdict lines, ten measured checks each (37345). | The new truncation guard has not itself been observed firing: §5.2. The Gemma tree's re-walk under the fix was attempted (37347) and returned nothing because of #193; with #193 landed, 37348 re-walked it to `adjudicated=2 of 2 ... ok=2 abstain=0 refuse=0`, both trees green on ten measured checks. MEASURED. |
| Run failure-path verdict mapper | MEASURED | A deliberate unrecognized flag on the passing arm's argv produced a real failure: sacct FAILED 5:0, raw rc and mapped code both recorded, missing verdict mapped to 5 RED rather than 95, END line last: 1/1 real failure (37346). Before the fix the same shape recorded CANCELLED 0:15 with no END line (37304; also 37336). | Positive control: the injected argparse error reached 8/8 ranks; the unmodified passing arm is unaffected (37341, 37342). | One failure shape measured (argparse rc 2 flattened to 1 by torchrun). Other failure shapes UNMEASURED. Its success-arm guard was drilled on one of the launcher's three arms (`train`, `resume`, `post-mortem`); on the post-mortem arm that guard exits 95 by construction and the shared adjudication tail below it is dead code: #193. |
| Post-mortem reporting link | MEASURED (regressed on the current artifact: #193) | The shipped link exited 0 over 0 of 3 adjudicated checkpoints with no verdict line (37343) — the detector existed and was never reached. | Fixed link reached the existing adjudication tail and printed its END line: 37344, COMPLETED 0:0 — true of the pre-#189 artifact. On the current artifact the same link exits 95 above the tail with `ADJUDICATE` 0 times (37347, #193). | The empty-tree guard is dead on the post-mortem arm until #193 lands: §5.2. |
| Comparator probe, synthetic logits | MEASURED | A deliberately perturbed input changed the label: 1/1 [ADJ]. Lossy-restore RED under #177 control C3: `restore_delta = 0.004999999999999893`. | Unmodified synthetic tensors compared bytewise over 4,874,240 of 4,874,240 elements [ADJ]. Real restore exactness: 37310, 37342. | No deliberately corrupted REAL checkpoint has been resumed: §5.2. |

### 5.2 Required controls without an observed MUST_FIRE

All rows below are **BLOCKED or UNMEASURED**, not certified.

| Control object | Status | Why there is no observed MUST_FIRE |
|---|---:|---|
| Dataset planted unreadable-input control | UNMEASURED | The real `phase3_real_4k` corpus now flows through both arms with token denominators, but no planted unreadable-input MUST_FIRE has been run. |
| Adjudicator truncation guard | UNMEASURED | The refuse-96-on-`processed≠found` guard is installed and its MUST_PASS side executed (37345, 3 of 3); no truncated tree has been fed to it since. |
| Post-mortem empty-tree guard | MEASURED at build time; UNMEASURED on a real empty tree | The guard was reachable on 37344's artifact, dead on 37347's (#193), and is reachable again on the current one (37348). `MUST_FIRE/EMPTY_TREE_STILL_95` now drives the post-image tail with an adjudicator stub observing ZERO checkpoints and observes exit 95 with the tail entered — the drill that stops #193's fix from reopening #187. What remains UNMEASURED is narrower than before and stated as such: no post-mortem link has been run against a genuinely empty checkpoint tree on the cluster; the 95 has been observed on a lifted fragment of the real artifact, not on a real allocation. |
| Real corrupted-restore comparator drill | UNMEASURED | Lossy-restore RED is demonstrated on the synthetic control C3 (#177); no deliberately corrupted real checkpoint has been resumed under the comparator. |
| Additional failure shapes of the verdict mapper | UNMEASURED | One shape measured (37346). OOM, preemption, node loss and hang shapes have no observed mapping. |
| Architecture-registration seam | BLOCKED | The seam does not exist. No valid registration MUST_PASS or invalid registration MUST_FIRE has executed. |
| Final-credential portability | BLOCKED | Credential delivery has not occurred; no leak, permission, or module-scope control has run under it. |

Additional detector-coverage gaps:

| Coverage object | Status | Boundary |
|---|---:|---|
| MoE expert-FQN handling | UNMEASURED | All executed models are dense: 0/2 MoE candidates. |
| Multi-node NCCL | BLOCKED | The estate exposes one node. Inter-node collectives cannot be exercised there. |
| Node-failure recovery | BLOCKED | No second node or induced multi-node failure surface exists. |
| Canonical-weight Gemma equivalence | UNMEASURED | Third-party mirror used throughout; canonical repository returned HTTP 401: §3.4. |

### 5.3 Control failures measured in Phase 4

Phase 4 found three cases where the plane's own reporting could report success over work it had not done, plus the class they belong to. Each is recorded before/fix/after.

**#187 — the reporting link that was never reached.** Before (37343): the post-mortem `afterany` link exited 0 having adjudicated zero of three checkpoints and emitted no verdict line at all; its entire log was one line, and sacct records COMPLETED 0:0 — indistinguishable from a clean sweep. This is a control failure, not a code style issue: the link exists precisely for the case where production died, and in that case it reported success over an empty denominator. The adjudication tail and its guards already existed; the link exited early and never reached them. Fix (fs187): set a launcher-internal skip flag, fall through backend init, runtime setup, the bind plane, the in-container GPU census and `fs_compose_launch`, skip only the one `run_in_container` call, and reuse the existing adjudication tail verbatim — same walker, same denominator, same empty-tree guard, same END line. After (37344): COMPLETED 0:0 in 3m40s, END printed with `phase=post-mortem`. That after-state was true of the artifact 37344 ran on; it is false of the current artifact, where the same link exits 95 above the tail it was fixed to reach — see #193 below.

**#188 — a denominator taken from the stream it measures.** Fixing #187 made 37344 print an END line for the first time, and the line read `checkpoint_saves_adjudicated=1` over a tree holding three checkpoint directories. The same `=1` appears in 37341 and 37342; a fourth observation is 37319's `checkpoint_dirs=1` over a two-checkpoint tree. Cause: the shipped walker's loop body inherited the loop's stdin and drained the `find` stream the loop was reading, so it adjudicated the first entry only — and then printed its count from the truncated stream it was measuring. Numerator and denominator were cut by the same defect, so the report stayed internally consistent at one third of the truth; a denominator taken from the stream it measures cannot detect its own truncation. The fix had been authored under #175, verified and committed, and wired into neither the stage list nor the publish set — the build kept regenerating the defective one-liner. Installed fix: collect into an array first, iterate second, redirect the adjudicator's stdin from `/dev/null`, count `found` and `processed` independently, and refuse 96 if they disagree. After (37345): COMPLETED 0:0 in 5m51s, `adjudicated=3 of 3 ... ok=3 abstain=0 refuse=0`, three `VERDICT 0 PASS` lines with ten measured checks each, and the END line prints `found` alongside `adjudicated` so the two can be compared without trusting either alone. **Every `checkpoint_saves_adjudicated=1` citation in this report is annotated as the truncated denominator wherever it appears; the corrected `3 of 3` stands where the tree has been re-walked (Qwen), and UNMEASURED where it has not (Gemma, 37319).**

**#189 — the failure path, measured with a deliberate failure.** The same question asked of the failure path found a third orphan, this one recorded as fixed three times over (#174, #169, #171). Measured on the current build before any change: the marker `fs175:` occurred 0 times in both generated artifacts, the launcher carried 0 traps, and the failure path handed the backend's hard-stop helper `"$$"` — the launcher's own pid. The helper's first act is `kill -TERM` on that pid; with no trap, bash takes SIGTERM's default action, so the END line and the `exit` below it were both unreachable, along with the helper's own container-cleanup arm. `|| true` does not help; the shell dies from the signal, not from an exit status. Before (37304, recorded): sacct CANCELLED 0:15 — a signal an orchestrator reads as a human cancel — 0 END lines, 0 FATAL lines, exit never reached. Fix (fs175, `patch_launcher_exit_discipline.py`, 676 lines, previously in neither list). After (37346), verified by causing a real failure — one unrecognized flag appended to the passing arm's argv, everything else identical: sacct FAILED 5:0; the END line is the last line and records both the raw rc and the mapped code; a missing verdict maps to 5 RED, not 95 UNMEASURED. Positive control: the injected error appears once per rank, 8/8.

**#190 — the publish direction of the same class.** Five build stages RAN but were absent from the publish set, so the published build script referenced five files a clone would not have — `patch_fabric_tripwire.py`, `patch_fsdp_wrap_policy.py`, `patch_master_addr.py`, `patch_nv_runtime.py`, `patch_train_phase_balance.py`, all real BLOCKER fixes. The existing coverage gate ran one direction only: it refused when a published line was in no scan category, and never asked whether a stage that runs is shipped.

**#193 — two correct fixes that compose to a dead branch.** The re-walk of the Gemma tree under the #188 fix was run as the post-mortem `afterany` link against `out_probe` (two checkpoint directories on disk) on the current artifact. After (37347): 8/8 srun steps COMPLETED, `FS_COLLECTIVE world=8 ... verdict=OK`, `fs129: collective probe PASS`, stderr 0 lines, launch log 51 lines — and `ADJUDICATE` occurs 0 times. The last line is `END rc=0 mapped_rc=95 phase=train FAILED (fs_map_run_verdict: verdict=NONE rc=0 mapped=95 -- no verdict line; ...)`; sacct records FAILED 95:0 in 2m36s. The arm the END line names is `phase=train` on a run whose `FS_PHASE` is `post-mortem`. Before (37344, the same link on the artifact as it stood after #187 landed and before #189 landed): `fs175` occurs 0 times, 71 lines, `ADJUDICATE complete root=<HOME>/fs-phase4-qwen/out phase=post-mortem checkpoint_dirs=1`, `END rc=0 phase=post-mortem checkpoint_saves_adjudicated=1`, sacct COMPLETED 0:0 in 3m40s. The adjudication tail was reached; one stage separates the two jobs. This is a regression, and it has a before and an after that were both actually run. Cause: #187 deliberately routes the post-mortem link through the shared adjudication tail — the post-mortem branch sets `FS_SKIP_TRAIN=1` and `rc=0`, and its own comment says the tail 'is what produces the verdict -- reused verbatim, not duplicated' — while the #171 success-arm guard, the one that refuses `rc=0` when the log carries no trainer verdict line (the vacuous-pass hole), sits above that tail and exits 95 before it. On the post-mortem arm no trainer ran, so there is no verdict line by construction: the guard's precondition is violated on every single invocation and the tail below it is dead code, while the banner the operator sees still says the tree 'is adjudicated below', which the shipped artifact does not do. The #171 guard reached the artifact only when #189 landed, because the stage carrying it was the #189 orphan. No gate caught it because there is no contradiction anywhere for a detector to see: 95 is the correct code for a run that measured nothing, and the exit code, the sacct state and the log all agree with each other and all agree that nothing was adjudicated. #189 shipped with controls — it drove the failure arm (37346) and the success arm — but the launcher enumerates its arms in one variable (`train`, `resume`, `post-mortem`), and #189 drilled one of three. **The rule: a fix that inserts a guard on a shared path owes a drill on every arm that path serves.** Fix, landed as build stage 34 (`patch_postmortem_verdict_scope.py`, wired into both membership lists per #191): scope the #171 mapper to `FS_SKIP_TRAIN != 1`; on the else arm, print a line recording that the trainer-verdict check was deliberately not applied and naming the adjudication tail as this arm's verdict source; let the post-mortem arm take its verdict from the tail, whose own refusals stand unchanged (`checkpoint_observed -gt 0 || fail 95`; fs176 `observed -eq found || fail 5`). Controls: MUST_FIRE/DEAD_BRANCH (the pre-image reproduces 37347), MUST_PASS/REACHES_TAIL, MUST_FIRE/EMPTY_TREE_STILL_95 (the load-bearing one — it stops the fix from reopening #187), MUST_FIRE/PARTIAL_COVERAGE_STILL_5, MUST_PASS/TRAIN_ARM_UNCHANGED. **CLOSED by measurement — job 37348.** All five controls fired at build time on the real tail lifted verbatim from the artifact: DEAD_BRANCH exit=95 with 0 adjudicate calls, REACHES_TAIL exit=0 with 1 call and an `END rc=0`, EMPTY_TREE_STILL_95 exit=95, PARTIAL_COVERAGE_STILL_5 exit=5, TRAIN_ARM_UNCHANGED exit=95 with 0 calls. The regenerated launcher was shipped and md5-verified on both sides, and the same post-mortem submit that produced 37347 was re-run as 37348: COMPLETED 0:0 in 5m13s, 91 lines, 0 stderr, `ADJUDICATE complete ... adjudicated=2 of 2 checkpoint dir(s) ok=2 abstain=0 refuse=0`, `END rc=0 phase=post-mortem checkpoint_saves_adjudicated=2 checkpoint_saves_found=2`. Three points on one arm: 37344 reached the tail, 37347 could not, 37348 does again. The deliberately-skipped check is on the record as a printed line on the else arm rather than inferred from its absence.

**The class.** #136, #188 and #189 are three instances of one failure mode: a fix authored, verified and committed while appearing in no execution list, so it never ran and never shipped. A fix in that state is indistinguishable from a fix that was never written. #190 is the same family in the publish direction. All six orphans are now wired in and the build was previously green at 33 stages, 36 today. The gate that closes the class's orphan direction (#191) has landed and is wired into both lists: it partitions 54 files and measures RUN_NOT_SHIPPED 0 — the direction that produced #189 and #190 is now empty by measurement rather than by assumption, and the orphan count is a gated number, not an audited one. #189 is now also the origin of a regression: the stage it landed carried the #171 guard that makes the post-mortem arm dead on the current artifact (#193). #191 does nothing about that direction — arm coverage is a different failure mode that happens to share a stage with the orphan class.

## 6. What is UNMEASURED and why

### 6.1 Phase 3

The genuine Phase 3 script has executed **eight of eight legs: 8/8** (job 37310; all eight legs `verdict:"MEASURED"`, no abstentions). Manual lower-level probes are no longer the evidence of record for any leg. What remains open inside Phase 3 scope:

| Residual item | Status | Missing measurement |
|---|---:|---|
| Corrupted-real-restore drill | UNMEASURED | A deliberately perturbed tensor in a REAL checkpoint, resumed under the comparator, observed RED. Only the synthetic control (C3, #177) has fired. |
| Time-limit guard (M4) | FAILED | An over-limit value refused and an in-limit value admitted. Not re-measured in Phase 4. |
| Iteration-budget consumers (M5) | FAILED | Both budget names consumed by the probe and controlling real step/save counts. Bounded runs have executed, but the variable-consumption defect itself has not been re-audited. |
| Launch-document generator (#127) | FAILED | The generator producing the launch document green on its own gate. |

### 6.2 Phase 4 and broader generalization

**The generalization boundary as it actually stands: two models executed — Qwen (Qwen3-4B and qwen3-0.6b) and Gemma-3-1b — both small, on one node, under one container runtime (singularity), on one dataset (`phase3_real_4k`).**

| Claim | Status | Evidence boundary |
|---|---:|---|
| Unqualified "FoundationScale is model-agnostic" | NOT LICENSED | What is licensed: the same framework revision, argv shape and corpus drove two different model families end to end, and the one divergence is attributable to the model because the identical code path produced exact rank agreement on the control — `env_phase4_qwen.sh` and `env_phase4_gemma.sh` carry character-identical trainer argv apart from the model path, including `--resume-tolerance 0.0005`, and at that same threshold Qwen measured `cross_rank_spread_before_save 0.0`, `cross_rank_spread_after_resume 0.0`, `restore_delta 0.0` (37342) while Gemma abstained at 0.2940967082977295 (37336). Control and abstaining arm are compared at one threshold. The denominator is two small models on one node; anything wider is UNMEASURED. |
| Canonical-weight Gemma equivalence | UNMEASURED | All Gemma measurements used the third-party mirror `unsloth/gemma-3-1b-it`; the canonical `google/gemma-3-1b-it` returned HTTP 401 and was never fetched. Closure: re-run the Gemma arm against canonical weights once access exists (§3.4). |
| Mechanism of the Gemma cross-rank divergence | UNMEASURED | Characterized, not root-caused: a subset of ranks computes a different, bit-stable loss from identical inputs and identical weights. Forensics ruled out the dataset, the loaded weights, restore fidelity, the attention backend, the sliding-window configuration, dropout, and missing state-dict keys, each with denominators (37320–37323). Naming the mechanism inside the model remains UNMEASURED. |
| #192 threshold separation | CLOSED at build time | Landed as stage 35: `--resume-tolerance` governs restore fidelity only, optional `--rank-agreement-tolerance` carries the cross-rank claim, and an unset cross-rank knob reports its spreads and their signed delta while declaring the absolute claim `unmeasured_cross_rank` — self-calibration never mints `rank_invariant`. 10/10 static gates, 8/8 controls. **Residual:** no cluster job has yet run it, so the new fields are unobserved on hardware. |
| #191 orphan-stage gate | MEASURED | The gate exists, is wired into both the stage list and the publish set, and runs green in the build: 54 files partitioned with RUN_NOT_SHIPPED 0 — the extent of the orphan class is now a gated number, not an audited one. What it does not measure: the 18 SHIPPED_NOT_RUN files, of which 4 are declared `runtime-artifact` (an attestation that they execute on the cluster, not a measurement of it), and the arm-coverage direction #193 exposes. |
| #194 doc/build count agreement | MEASURED, CLOSED for the count | The stage count is now derived by `gate_doc_stage_count.py` rather than retyped: 16 claims over 5 documents, 7 agreeing at 36, 9 declared historical, 0 stale, 5/5 controls. The narrative status lines corrected in the same pass are not machine-checked, and that residual is stated in E. |
| ~~#193 post-mortem arm dead on the current artifact~~ | CLOSED — now MEASURED | Three points on one arm: 37344 (pre-#189) reached the adjudication tail, COMPLETED 0:0; 37347 (post-#189) reached `ADJUDICATE` 0 times and exited 95 above the tail, sacct FAILED 95:0; 37348 (post-#193) reached it again, `adjudicated=2 of 2`, COMPLETED 0:0. The Gemma tree's checkpoint count is MEASURED, and the third point is what proves the fix scoped the guard rather than deleting it — `MUST_PASS/TRAIN_ARM_UNCHANGED` and `MUST_FIRE/EMPTY_TREE_STILL_95` are in the build the artifact came from. |
| #180 invocation record | CLOSED at build time | Landed as stage 36: the launcher writes a provenance record next to its log before exec, on the training arm only, holding the raw and composed command and every `FS_` name the shell held — exported or not. `10/10` static gates, `8/8` controls, including a census-denominator drill that goes red on the pre-image. Residual: unrun on hardware, so the field this report can cite is still the pre-image's `0 occurrences`. |
| MoE checkpoint machinery | UNMEASURED | No MoE checkpoint exists; the expert-FQN logic has not been exercised by a real model: 0/2. |
| Out-of-tree Gemma-4 architecture | BLOCKED | Both candidates require an architecture-registration seam: 2/2 [E.1; #119]. |
| Multi-node distributed training | BLOCKED | Exactly one node is available [M11]. |
| Inter-node NCCL | BLOCKED | All measured collectives are intra-node. |
| Node-failure recovery | BLOCKED | No second node on which to induce or survive a node failure. |
| Throughput and convergence generality | UNMEASURED | Throughput is measured only for the executed models at the executed sizes (5041.991/3731.470 tokens/s, 37310; 5517.637/4527.572, 37319; eval 519.157 tokens/s, 37310). No convergence claim is made. |
| Dataset correctness beyond one corpus | UNMEASURED | One dataset (`phase3_real_4k`) is the denominator; no second dataset, and no planted unreadable-input MUST_FIRE. |
| Framework evaluation accuracy | UNMEASURED | Held-out loss with token denominators is measured (37342, 37319); no labeled corpus, reference answer set, or score denominator exists. |
| Final-credential equivalence | BLOCKED | Permissions, quotas, host user-site state, and module availability are credential-scoped and must be rerun under the authorized credential. |
| Current 36-stage build determinism | UNMEASURED | The recorded byte-identical result belongs to the earlier 17-stage/five-artifact build; no double-build hash comparison exists at 36 stages [E.5; CUR-BUILD]. |

## 7. What must happen next, in order

| Order | Required action | Current status | Named blocker | Acceptance evidence |
|---:|---|---:|---|---|
| 1 | Land the #192 fix: split `_resume_continuity_verdict`'s single tolerance into two knobs. | LANDED (build stage 35) — **next: run it.** | One scalar guarded both restore fidelity and rank agreement; the split is certified but has not executed on the cluster. | Re-run the Gemma arm at `--resume-tolerance 0.0005` with `--rank-agreement-tolerance` left unset and observe `cross_rank_spread_delta` populated on real hardware, the status still `unmeasured_cross_rank`, and the Qwen control still exact. Note the landed rule departs from the one named here: self-calibration reports the floor but may never certify against it. |
| 2 | ~~Land the #193 fix~~ **DONE**: scope the #171 verdict mapper to `FS_SKIP_TRAIN != 1`, with the else arm recording that the trainer-verdict check was deliberately not applied and naming the adjudication tail as this arm's verdict source; then re-walk the Gemma tree. | CLOSED | On the current artifact the guard exits 95 above the shared adjudication tail on every post-mortem invocation (37347); the tail is dead code on that arm, and the Gemma tree's checkpoint count is UNMEASURED until this lands. | The five named controls are observed — MUST_FIRE/DEAD_BRANCH (pre-image reproduces 37347), MUST_PASS/REACHES_TAIL, MUST_FIRE/EMPTY_TREE_STILL_95, MUST_FIRE/PARTIAL_COVERAGE_STILL_5, MUST_PASS/TRAIN_ARM_UNCHANGED — and the Gemma-tree re-walk prints `ADJUDICATE complete` over its two checkpoint directories. **Met**: five of five controls fired; job 37348 printed `ADJUDICATE complete root=<HOME>/fs-phase4-gemma/out_probe phase=post-mortem adjudicated=2 of 2 checkpoint dir(s) ok=2 abstain=0 refuse=0`. |
| 3 | Close #180: record the raw and composed launch command in the run log. | LANDED (build stage 36) — **next: run it.** | Zero echo sites measured on the pre-image; `10/10` static gates and `8/8` controls on the patched launcher. | The record ships as a sibling JSON file rather than as log lines, because the campaign's actual failure was reconstruction from a 936-line log, and grep-ability was the thing missing. Success on hardware: a job whose `logs/launch.<jobid>.provenance.json` exists, whose `launch_cmd_composed` re-runs, and whose `fs_env` carries both bare resume knobs. |
| 4 | Re-run the Gemma arm against canonical `google/gemma-3-1b-it` weights. | BLOCKED | Canonical repository returned HTTP 401 to this estate's credentials. | Same argv (`phase4_gemma.json`, `--resume-tolerance 0.0005`) against canonical weights; the 0.2940967082977295 spread reproduces or does not, recorded with the same denominators (§3.4). |
| 5 | Repair M4 and M5. | FAILED | M4 cannot reject invalid limits; M5's two budget names have no recorded consumers. | An over-limit value refused and an in-limit value admitted; both budget names consumed and controlling real step/save counts. |
| 6 | Land the architecture-registration seam (#119). | BLOCKED | The seam does not exist: 2/2 out-of-tree candidates refused. | A valid registration MUST_PASS and an invalid registration MUST_FIRE both execute. |
| 7 | Double-build determinism at 36 stages. | UNMEASURED | No current-build hash comparison exists. | Two consecutive complete builds produce byte-identical hashes for every artifact, with the artifact denominator stated. |
| 8 | Receive the authorized credential; push immutable artifacts; repeat credential-scoped measurements. | BLOCKED | Credential must come from the requester's secure channel. | An authorized non-interactive push succeeds; runtime inventory, host user-site, image import, mount, and torch-provenance controls rerun under the authorized credential with unchanged artifact hashes. |
| 9 | Run the real-dataset negative controls and the corrupted-real-restore drill. | UNMEASURED | Planted unreadable-input MUST_FIRE and a deliberately corrupted real checkpoint resume have not been observed. | The dataset gate fires on a planted unreadable input; the comparator goes RED on a perturbed real tensor, then reports restored equality as `k of N` with `N > 0`. |
| 10 | Extend distributed claims only when the hardware denominator changes. | BLOCKED | Only one node exists on the measured estate. | Inter-node NCCL and node-failure claims require at least two nodes under the final credential and a MUST_FIRE for a lost or wrong-rank contribution. |

**Bottom line:** the measured evidence now establishes FoundationScale training on H100s at single-node scope: Phase 3 executed 8/8 legs through the shipped launch path (37310), the full submission chain completed end to end with every link COMPLETED 0:0 (37340–37343), resume is exact on the measured control (restore_delta 0.0, 0 of 513 optimizer mismatches, 37342), and real checkpoints adjudicate 3 of 3 under a walker whose denominator can no longer truncate silently (37345). Two model families ran the same argv on the same corpus; the single divergence abstained rather than passing and is attributable to the model, subject to the third-party-mirror provenance caveat, which leaves canonical-weight equivalence UNMEASURED. The re-baseline also measured three control failures in the plane's own reporting — a reporting link that exited 0 over zero adjudicated checkpoints, an adjudicator that walked one third of its tree and called it complete, and a failure path that killed its own shell — all three fixed and re-measured (37344, 37345, 37346), with the first regressed on an intermediate artifact, where #189's success-arm guard killed the #187 post-mortem arm on every invocation (#193, 37347), and then closed by measurement one stage later (37348: `adjudicated=2 of 2 ... ok=2`), which also lands the Gemma tree's checkpoint count as MEASURED; and all three instances of one orphan-fix class whose orphan direction is now closed by measurement: gate #191 exists, is wired into both lists, and reports RUN_NOT_SHIPPED 0 over the 54 files it partitions. No finding is OPEN. #180 is CLOSED at build time — stage 36 makes the launcher write its own invocation to `logs/launch.<jobid>.provenance.json` before it execs, over a census that counts every `FS_` name the shell held rather than only the exported ones, certified 10/10 static gates and 8/8 controls with the census drill observed red on the pre-image — and it carries the same residual as #192: no job has written one. #192 is CLOSED at build time — stage 35 splits the one scalar that decided both restore fidelity and rank agreement, certified 10/10 static gates and 8/8 controls with `MUST_FIRE/RESTORE_RED_SURVIVES_WIDE_RANK_KNOB` observed `red` where the pre-image read `pass` — and its residual is a job, not a fix: the split trainer has not yet run on hardware. Certification of FoundationScale as a generalizable foundation-model training framework remains **unachieved**: the denominator is two small models, one node, one container runtime, one dataset.
