# Verification Matrix — FoundationScale

## The measurement that shapes everything

This matrix exists because of one measurement, taken before any of it was built, and the paragraphs below state that measurement in the past tense on purpose: #373 has since changed most of its numbers, and the next section says exactly which. The zeroes are kept rather than overwritten because they are the reason this matrix exists at all.

The instrument was a grep over `src/foundationscale` (excluding `rl/`), read against the code. The measurement: FoundationScale's training plane does NOT implement an inner loop. It builds a `transformers.Trainer` (`src/foundationscale/train/loop.py:2223`) out of `TrainingArguments`, from a kwargs dict (`src/foundationscale/train/loop.py:2103`) whose ENTIRE content was:

    output_dir, max_steps, per_device_train_batch_size, learning_rate,
    save_strategy, save_steps, seed, logging_steps, report_to,
    ddp_find_unused_parameters, (bf16|fp16) from cfg.precision, and
    save_safetensors when the installed transformers accepts it

TrainConfig's full field set was: model, dataset, output_dir, nodes, gpus_per_node, profile*, objective, precision, adapter*, max_steps, per_device_batch_size, learning_rate, save_interval, seed, dp/tp/pp/ep/cp, dry_run, launch_corpus.

The grep counted, at that time:

- `attn_implementation|flash_attention|sdpa` → 0 occurrences
- `torch.compile` → 0
- `get_scheduler|lr_scheduler|warmup` → 0
- `AdamW|Adafactor|SGD(` → 0
- `deepspeed|FSDP|fully_shard|ZeRO` → 2, both in `gates/` + `provenance/` (adjudicated/recorded, never built)
- `clip_grad|max_grad_norm` → 0

The consequence, and it is the whole point: every named axis — optimizer, CPU offload, DeepSpeed, gradient checkpointing, gradient accumulation, flash attention, LR schedule — was NOT absent from the RUN. HF Trainer supplies a default for each. It was absent from the DECLARATION. The run used `adamw_torch_fused`, accum=1, max_grad_norm=1.0, no recompute, linear LR with zero warmup, and whatever attention implementation the model config picks — and the manifest could not say so, because there was no config key to resolve and record.

Those six values are measured, not assumed: they are the `TrainingArguments` defaults read out of transformers 5.16.1. Stating the version is not pedantry — it is the whole shape of the problem. An undeclared axis takes its value from the installed engine, so the same FoundationScale config trains differently under a different transformers release, and no artifact of either run would record that anything changed. Note in particular that the optimizer default is the *fused* AdamW, not the reference one: T1-9 proposes to measure step time across optimizers, and it would be measuring against a fused baseline without ever saying so.

That is the #346 class at scale: unmeasured reads as green. It is why, when this matrix was written, 14 of its 33 rows were unrunnable.

## What #373 changed, and why the grep counts above no longer hold

#373 shipped tier 0, so **every count in the list above is now stale by design.** They are corrected here rather than edited in place because a matrix that quietly rewrites its own premise cannot be audited against the tree it described.

Re-measured on the same instrument, over the same scope, on the same day this section was written: `attn_implementation|flash_attention|sdpa` → 31 occurrences, `get_scheduler|lr_scheduler|warmup` → 26, `clip_grad|max_grad_norm` → 14, `deepspeed|FSDP|fully_shard|ZeRO` → 9 (the original two in `gates/` and `provenance/`, plus seven in `train/`), `AdamW|Adafactor|SGD(` → 3. Only `torch.compile` is still zero: nothing declares it, and it remains T1-15's business.

`TrainConfig` now carries nine tier-0 axes — `optimizer`, `gradient_accumulation_steps`, `max_grad_norm`, `gradient_checkpointing`, `attn_implementation`, `lr_scheduler_type`, `warmup_steps`, `sharding_strategy`, `cpu_optimizer_offload` — each resolved through `ConfigResolver` and recorded. Each is bound into the kwargs dict only when the operator declared it, so an undeclared axis still takes the engine default; what changed is that the ABSENCE is itself recorded, which is precisely the distinction the original measurement said no artifact could make.

Two of the nine are declarable without being executable, and the matrix says so rather than counting them as capability: `sharding_strategy=fsdp|zero3` and `cpu_optimizer_offload=true` REFUSE with 96 rather than accept a declaration this plane cannot honour. That is why T1-13 and T1-14 read REFUSED and not UNBLOCKED — an unrunnable arm is not the same fact as a missing one.

## Two tiers, and tier 0 is not optional

**Tier 0** (no GPU, blocks everything below): make each axis DECLARABLE and RECORDED. A knob that is exposed but unrecorded is worse than absent, because a run then makes a claim no artifact can check. Every tier-0 row is:

- (a) a TrainConfig field,
- (b) bound into the TrainingArguments kwargs,
- (c) resolved through ConfigResolver so `provenance/manifest.py` records key/value/source,
- (d) a control proving an off-declaration run REFUSES rather than silently training on the engine default.

**Tier 1** (GPU, a 4-GPU tray): one row per claim. Every row carries a CONTROL ARM — an arm that must come out DIFFERENT.

Tier 0 blocks tier 1 because a tier-1 row that toggles an undeclared axis measures nothing attributably: the run would train on the engine default either way, and no artifact could distinguish "the knob worked" from "the knob was never wired." Rows T1-9 through T1-14 each named the tier-0 row they waited on. #373 shipped all of those, so each of the six now names what released it: T1-9 through T1-12 have since RUN on a tray -- T1-9, T1-10 and T1-11 GREEN, all three re-taken at the same shipping commit so the three GREENs are attributable to one tree rather than to three ancestors, and T1-12 UNMEASURED because the flash_attn module is absent from the tray, which is the row refusing to report a two-arm result as its three-arm claim -- while T1-13 and T1-14 are REFUSED — the dependency shipped as a refusal, so their run arms have nothing to execute.

## The matrix

### T0 — declaration and recording (CPU, minutes each)

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T0-1 | optimizer name is declared and recorded | build with optimizer=adamw_torch | declaring an unsupported optimizer REFUSES 96, does not fall back | 0 GPU | SHIPPED (#373): declared optimizer reaches TrainingArguments.optim; an unsupported name REFUSES 96 before a Trainer exists |
| T0-2 | grad accumulation steps are declared and recorded | accum=4 | kwargs actually carries gradient_accumulation_steps=4 (introspect args) | 0 GPU | SHIPPED (#373): reaches gradient_accumulation_steps in the kwargs; an omitted axis adds no kwarg and is recorded as an explicit absence |
| T0-3 | max_grad_norm is declared and recorded | 1.0 vs 0.0 | args.max_grad_norm reads back what was declared | 0 GPU | SHIPPED (#373): reaches max_grad_norm in the kwargs; a non-positive value REFUSES 96 at the CLI boundary (#384) |
| T0-4 | gradient checkpointing (recompute) is declared and recorded | on | model.is_gradient_checkpointing is True (not just the flag set) | 0 GPU | SHIPPED (#373): model.is_gradient_checkpointing reads True on the loaded model, not merely the flag |
| T0-5 | attention implementation is declared and recorded | sdpa | model.config._attn_implementation == declared; an impl the build cannot supply REFUSES | 0 GPU | SHIPPED (#373): reaches model.config._attn_implementation; an impl the build cannot supply and one the loader silently overrides both REFUSE 96 |
| T0-6 | LR schedule and warmup are declared and recorded | cosine, warmup=2 | the LR at step 1 != LR at step 10 (read the log) | 0 GPU | SHIPPED (#373): the declared schedule and warmup move the optimizer LR -- early below half the base rate, late above 0.85 of it |
| T0-7 | sharding strategy (ddp\|fsdp\|deepspeed) is declared and recorded | ddp | a strategy with no wiring REFUSES 96 rather than silently running DDP | 0 GPU | SHIPPED (#373): declared and recorded; fsdp/zero3 REFUSE 96 rather than silently running DDP, while ddp and undeclared proceed |
| T0-8 | CPU optimizer offload is declared and recorded | off | declaring on with no backend REFUSES | 0 GPU | SHIPPED (#373): declared and recorded; offload=true REFUSES 96 with no backend, while false and undeclared proceed |
| T0-9 | code.status is a real commit | run from a git checkout | #346: a COPY must record status=not_a_repository AND the run must say so loudly, not proceed silently | 0 GPU | SHIPPED (#488, #487, #346): found RED by this matrix's own harness and FIXED, not published as a defect. The recording half was never wrong -- an unversioned tree is recorded as status not_a_repository with commit null in the written run_manifest.json, while a real checkout records the commit an independently read HEAD reports -- so nothing was ever misattributed. The loudness half was: nothing outside the provenance module read code.status, so a run unpacked from a tarball exited 0 over 11 lines without one of them saying it was unattributable. _emit_manifest now says so whenever the recorded commit is absent, and the control run from a real checkout stays silent, which is what keeps the line worth reading |
| T0-10 | the effective topology is read from runtime evidence, never echoed from the config | WORLD_SIZE=8 LOCAL_WORLD_SIZE=8 against a config declaring tp=pp=ep=cp=4 and gpus_per_node=4; and on hardware, four real srun/torchrun arms inside one allocation: 4 declared/4 launched, 16/16, and the deciding 16 declared/8 launched | #375: the pre-fix _effective_topology, which sourced 5 of the 7 fields from cfg -- 11 of the 14 legs in tests/train/test_effective_topology.py go RED against it and 14 of 14 pass after; on hardware the two truthful arms must disagree with each other (equal worlds score RED as a constant, not green) and a no-torchrun arm must announce the comparison skipped and report no world at all | 0 GPU for the unit legs; ~4 min on 4 nodes x 4 GPUs for the hardware arms | SHIPPED (#489, #375): 14 of 14 unit legs green with 11 RED on the pre-fix tree, and since #489 the claim is also measured end to end on four trays and sixteen GPUs through the shipped CLI. Handed a declaration of dp=16 against 8 launched ranks the run reported 8 -- the launch -- blocked on topology.effective_overrides_dp and topology.effective_overrides_nodes before importing torch, and wrote both the stage and the codes into its manifest. Two truthful arms answered 4 and 16, so the reading is not a constant. Adjudicated by a pure verdict whose 20 controls include a GREEN one for an uninformative exit code, because torchrun collapses worker exit codes and the arm is scored on what it said and wrote rather than on what srun returned |

### T0 — performance and measurement (CPU, minutes each)

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T0-11 | MFU/TFLOPs charge the ACTIVE routed experts, not the total expert count | build FlopsModel from the real Qwen3.5-35B-A3B and gemma-4-26B-A4B configs, and from a live module tree carrying BOTH expert storage layouts -- the indexed experts.<i> form and the fused, unindexed mlp.experts.gate_up_proj / .down_proj form | a DENSE model's flops_per_token must come out BIT-IDENTICAL to the pre-#529 value, not merely close, or every published dense number silently moves; the real gemma-4-31B config, which states num_experts: null, must stay on that dense path rather than being read as a Mixture-of-Experts model with an unknown count; and a model that declares experts but no per-token count must report UNMEASURED carrying its reason, never a dense number wearing a measured label | 0 GPU -- config and module-tree arithmetic, seconds. The end-to-end MFU figure this feeds additionally needs a tray and a declared device peak. | MEASURED GREEN on the formula (#529). The defect was real and large: FlopsModel charged 6 * total non-embedding parameters with no concept of routing, overstating MFU by the total/active ratio -- 6.51x on Qwen3.5-35B-A3B, measured, not estimated. Two of the three corrections came only from reading real checkpoints rather than reasoning about them. First, gemma-4-31B is DENSE and states num_experts: null, so a hasattr/key-presence test classifies it as MoE-with-unknown-count and destroys its MFU; the count must be keyed on the VALUE being a positive int, and bool is excluded because a flag is not a count. Second, and this one I shipped before catching it: the detector required an INDEXED experts.<i> segment, but the Qwen3.5-35B-A3B language model FUSES all 256 experts per layer into two unindexed stacked tensors -- 768 indexed tensors live under mtp. and ZERO under model. -- so the detector found 0 routed parameters in a model that is overwhelmingly experts by weight and the whole correction silently did nothing on the exact checkpoint it was written for. It matches the experts path SEGMENT now, and the test pins both layouts with the real tensor names. The shared expert is excluded because it runs for every token; on the measured checkpoint no tensor matches both patterns, so that guard is true by construction rather than by luck of naming, and the mutation row against it is a provably EQUIVALENT mutant rather than a test gap -- investigated, not papered over. Enrolled in the mutation battery in the same change, promoting perf/telemetry.py out of PENDING_ENROLMENT under the exit condition its own pending reason had named: 7 MUST-FIRE rows plus a MUST-PASS control. WHAT IS NOT MEASURED HERE: no end-to-end MFU number on hardware. The device peak is UNMEASURED until an operator declares it -- for this estate's GB200 that is 2,500 TFLOPS BF16 DENSE, not the 5,000 headline that includes sparsity -- and gemma-4-26B-A4B declares 128 experts with no per-token count under any spelling, so its MFU is now correctly UNMEASURED rather than 6x overstated. That is the right answer, not a complete one. |
| T0-12 | the suite's verdict does not depend on the order its files are collected in | run tests/provenance alone, then run it in the same process after tests/tooling, and compare the two verdicts | a green FULL-SUITE run must not be accepted as settling this: full-suite collection order happens to hide the pollution, so the ordered pair is the measurement and the full run is precisely the reading that must NOT be trusted here. The differential also has to be taken on a throwaway worktree of committed HEAD, or a failure in the working tree cannot be told apart from one the working tree introduced. | 0 GPU, seconds | MEASURED RED (#528), and PRE-EXISTING rather than introduced. tests/tooling leaves module state behind that makes tests/provenance fail when it runs afterwards, while the same files pass alone and the full suite passes end to end -- so the build is green for a reason that has nothing to do with the code being correct. Verified as pre-existing on a throwaway worktree of committed HEAD, which showed 5 such failures against 4 in the working tree at the time: the staged work did not cause it and does not depend on it. Recorded RED rather than fixed in the same pass, deliberately -- the fix is module-state hygiene in the tooling tests and belongs to its own change, and a defect that only appears under an ordering the suite does not currently exercise is exactly the kind that gets quietly absorbed if it is not written down first. No pytest-randomly or ordering job is wired yet, so nothing in CI would catch a regression of this class today; that gap is part of what this row records. |
| T0-13 | the quadratic attention term is charged PER LAYER, at the span each layer actually attends over | build FlopsModel from the real Qwen3.5 3:1 hybrid layout -- 27B declares 16 full-attention layers of 64 in text_config.layer_types -- and from a sliding-window layout with a declared window narrower than the sequence, and read the span the model reports and the FLOP count it produces | a config declaring no layer_types must come out BIT-IDENTICAL to the pre-#530 number, written in the test as the old expression rather than read back off the model, or every dense MFU figure already published moves; a config whose layer_types are entirely unrecognised must report the span UNDECLARED rather than reconstruct layers * sequence_length through a dictionary default, because that reconstruction is the right number by accident wearing a measured label; a declared span of exactly zero -- every layer linear -- must not be read as undeclared; and a boolean sliding_window must be rejected rather than charged as a window of one token | 0 GPU -- config arithmetic, seconds. The MFU figure this feeds still needs a tray and a declared device peak. | MEASURED GREEN (#530). The defect was real but small at the shapes measured so far, and it is the opposite sign to #529: flops_per_token charged 12 * layers * hidden_size * sequence_length, i.e. it assumed every layer attends over the whole sequence, so it OVERSTATED the quadratic term and therefore UNDERSTATED MFU. Two of the families this framework exists for violate that assumption. Qwen3.5 is a 3:1 hybrid whose linear-attention layers carry no quadratic cost at all -- 27B declares 16 full-attention layers of 64, so the term was charged about 4x -- and Gemma4 interleaves sliding-window layers whose key-span is bounded by the WINDOW rather than by the sequence. At the shapes run on this estate the quadratic term is about 3.5% of the total, so the correction is worth a few percent of reported MFU today; it is NOT a few percent at long context, which is the regime the benchmark mandate explicitly targets, and that is why it was fixed before those runs rather than explained after them. The scope came from checking rather than from the audit that prompted it: the audit framed this as a Qwen3.5-hybrid problem, and grepping the installed transformers 5.17.0 configuration modules for the literals that appear beside layer_types turned up sliding_attention and chunked_attention next to full_attention and linear_attention, which made it a per-layer span table rather than a count of full-attention layers. UNDECLARED IS A FALLBACK HERE, NOT A REFUSAL, and that asymmetry with #529 is deliberate: an unresolvable expert count overstates MFU about 6x and must abstain, while this term is 3.5% of the total and errs downward, so taking MFU offline for every hybrid model over it would be disproportionate. The acceptance rule is correspondingly strict -- list or tuple, every entry a string, length equal to the layer count, and at least one recognised entry -- so a partly-understood config gets the honest fallback instead of a half-applied correction. Two traps were caught in a standalone prototype before they reached the tree, each of which passes every other test written here: writing the fallback as `span or layers * sequence_length` reads an all-linear model's legitimate span of zero as undeclared and substitutes the naive value, a 100% error in the FLATTERING direction, which is why the field is int \| None and the test is `is None`; and reading sliding_window with a bare getattr accepts a bool, because isinstance(True, int) is True in Python, charging every sliding layer a span of one token. 20 tests, and the mutation battery was extended in the same change rather than after it: 7 new MUST-FIRE rows on 7 fresh anchors, measured 14 killed / 0 alive / 0 unscored / caught=100% over the module with its MUST-PASS control surviving and the source restored byte-for-byte. The two load-bearing traps are each killed by exactly one test -- the one written for them -- which is what distinguishes a test that pins a rule from a test that happens to pass. All five pinned corpus denominators were recomputed from the tools rather than predicted, and bumped in this same commit because adding rows without bumping them voids the shard. WHAT IS NOT MEASURED HERE: still no end-to-end MFU number on hardware, and the sliding-window path has been measured against declared configs rather than against a Gemma4 checkpoint read off disk, which is the reading T0-11's device-peak gap and this row both wait on a tray for. |

### T1 — precision (GPU)

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T1-1 | fp32 masters change the update magnitude relative to bf16 | fp32 masters | bf16 masters, same seed/lr/steps | ~15 min x2 | MEASURED (#367/#368: bf16 discards ~99.99%) — re-take DONE under the new manifest on a GB200 tray and it REPRODUCES: with precision the only axis changed, the fp32 arm moved 43,001 of 43,008 `input_layernorm` elements (99.98%) against 28 of 43,008 (0.065%) at bf16, and 100% against 50.3% of one 2-D `q_proj`. Cause: `--precision bf16` loads parameters AS bf16, so AdamW has no fp32 master copy and a sub-ULP update is discarded rather than accumulated — 29 norm elements had moved at step 10 and 28 at step 20 (#462) |
| T1-2 | fp16 grad scaler skips an overflowing step | fp16 | inject an overflow; scaler must SKIP, loss must not become nan | ~15 min | MEASURED and GREEN on a GB200 tray, with a control that could have fired and did. History: this row read "never run". The first thing the adjudicator had to settle is what the row is even about, because FoundationScale constructs no grad scaler anywhere -- a grep for GradScaler over src/foundationscale returns nothing. What it does instead is declare precision by setting the fp16 flag on a transformers TrainingArguments inside train/loop.py, after which transformers and accelerate own the scaler. So this row is a claim about the composed system, and the adjudicator says so in its own docstring rather than pretending the framework implements the skip itself. It runs three things. A declaration pin parses the installed foundationscale.train.loop with ast -- located through importlib find_spec, so it pins the package that would actually run rather than a file that happens to sit near it on disk -- and asserts the mapping from precision fp16 onto the fp16 flag is still there; without it the row would stay green while measuring only transformers, which is the failure mode of every test that reproduces a literal instead of exercising a code path. The pin is controlled both ways before being trusted: against the real source it reports the mapping present, and against a shadowed package whose mapping has been deleted it reports it absent, because a pin that cannot fire is decoration. A wiring arm then builds that declaration, drives a Trainer to the point where accelerate materialises its scaler, and resolves the live object instead of hand-building one. Finally two behavioural arms run the identical sequence through that one live scaler with the injected gradient as the only difference between them, so a divergence in their outcomes is attributable to the overflow and to nothing else. Measured on the tray: the scaler resolved at trainer.accelerator.scaler as a torch.amp.grad_scaler.GradScaler, enabled, at an initial scale of 65536. The run arm's finite gradient of 0.5 was scaled to 32768.0, unscaled back to exactly 0.5, and the parameter moved 1.0 to 0.5, so the step was taken and the scale was left alone. The control arm's infinite gradient survived as inf through both scale and unscale, the parameter stayed at exactly 1.0, the step was skipped, the scale was halved to 32768.0, and nothing became nan. The comparison is exact equality rather than a tolerance because whether the optimizer stepped is a discrete question, and a tolerance would only make room for a shrunken write to be scored as a skip. The verdict order refuses before it judges: 95 if the framework's loop module is not locatable here at all, because then its behaviour is absent rather than wrong; 5 if it IS locatable and no longer makes the declaration, because nothing downstream can skip an overflowing step and the claim fails upstream of the scaler; 96 if no scaler object resolves, since that is an instrument that cannot find what it tests; 96 if the two arms did not start from the same parameter value, since that is two instruments rather than one run twice; then 5 for a declaration that produced a disabled scaler, 5 for a control that did not skip, 5 for a run arm that DID skip -- a scaler that skips everything would pass the control while being useless, so both directions are required -- 5 for nan, and 0. The self-test drives that pure verdict function over synthetic payloads with no torch, no GPU, no network and no filesystem, and passes 17 of 17 controls, with a MUST-FIRE for every branch above and four that pin the ORDER, including one asserting that a missing framework outranks a missing scaler and one asserting that a dropped declaration is reported as the declaration failure rather than as the control failure it also causes. Honest limitation, stated rather than buried: this measures the handoff, not a skip inside a live training step. It establishes that the declaration is still made, that the declaration buys an enabled scaler, and that the scaler behaves correctly in both directions when handed an overflow. Watching the skip happen inside a real forward and backward over a 7.94B-parameter checkpoint is a separate and far more expensive reading, and this row does not yet carry it. |
| T1-3 | a 4-bit (nvfp4) declaration REFUSES, never falls back to bf16 | precision=nvfp4 | assert rc==96 AND no checkpoint written | ~2 min | MEASURED GREEN on a tray at 1460560 (#478), adjudicated by t1_3_nvfp4_refusal_arms.py, whose twenty-one self-test controls pin the verdict order. Taken twice: once against the estate's working checkout and once against a pristine clone of main at 55a1bb6 with a clean worktree, because the working checkout carries an uncommitted local patch and a number published from a modified tree is not a number about this repository. Both takes agree arm for arm. This row previously asserted its refusal with no adjudicator and was the last one doing so. Exit codes alone could not have settled it: this plane reaches 96 from two independent channels -- the config validator rejecting a precision outside PRECISIONS, and the training loop refusing nvfp4 specifically -- and nvfp4 clears the validator, so a bare rc==96 is equally consistent with a rejected declaration, an argument-parser error and a real refusal. Each arm is therefore scored on WHICH channel spoke. The deciding arm exits 96 from the loop's own refusal site naming the absent backend, trains nothing, writes ZERO checkpoint artifacts and records stage=refused with precision=nvfp4, satisfying this row's declared control arm in full. The same fixtures at bf16 exit 0, train to completion, write 7 checkpoint artifacts and record an observed dtype histogram of 338 BF16 tensors against the declaration -- so the absence of weights beside the refusal is a measured emptiness, not an untested one. A third arm shows the refusal fires even when the model cannot resolve, and a fourth shows that an unresolvable model is otherwise fatal (exit 5), which is what makes the third evidence of ordering rather than indifference. Unlike the declared video and audio rows, the declaration surface here reaches the refusal that exists. |

### T1 — checkpoint lifecycle (GPU)

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T1-4 | reloaded weights are bit-identical (save->load parity) | save then load, compare_keys EXACT | perturb one tensor -> parity must go RED | ~15 min | MEASURED and GREEN on a GB200 tray, with a control that could have fired and did. History: this row read "verify/parity.py SHIPPED, no run", and that was literally true -- src/foundationscale/verify/parity.py is a 1053-line library with no argparse, no main and no __main__ block, so nothing outside the unit tests had ever pointed it at a checkpoint that the framework itself wrote. A shipped comparator that has never compared a real artifact is a declaration, not a measurement. The row now carries an adjudicator, t1_4_save_load_parity.py, which runs two arms over a real on-disk safetensors checkpoint and decides from the compared counts alone. The identity arm is a genuine round trip and not a self-comparison: it copies the checkpoint, loads and re-SAVES every shard through safetensors with its metadata, and compares that against the ORIGINAL bytes. Comparing a checkpoint against itself would ask only whether one file reads the same way twice, which is very nearly a tautology, whereas the row claims save then load, so the arm has to write. The control is exactly one element of exactly one tensor stepped by +1 ULP, the smallest nonzero perturbation the dtype can represent, chosen because anything larger cannot distinguish an exact comparator from a merely tight one -- a detector that only notices a big change would pass this row while being blind to the corruption it exists to catch. Both arms are materialised by the same re-save routine, so the single perturbed element is the ONLY difference between them and a divergence in their verdicts is attributable to it and to nothing else; without that symmetry the perturbed arm would be the only one carrying re-save artifacts, and a dtype or metadata change on write would be scored as the control firing when the control had in fact not been tested. The tolerance policy is strict by construction: max_abs_diff 0, cosine at least 1.0, relative Frobenius 0. Measured against the adapter checkpoint that the T1-21 image arm saved through the framework's own save path, the identity arm compared 98 keys and 1,511,424 elements and came back ok, and the perturbed arm compared the SAME 98 keys and the SAME 1,511,424 elements and came back not ok, catching the step at element 0 of base_model.model.model.language_model.layers.0.self_attn.q_proj.lora_A.weight where 0.004369848873466253 became 0.004369849339127541 -- a last-bit change in fp32. So the comparator is exact and it is not blind, which are two separate claims and both are needed. The verdict order is load-bearing and refuses before it judges: 96 if the two arms did not compare the same key set, because that is two instruments rather than one instrument twice; 96 if the identity arm compared ZERO elements, because an empty comparison is the all-of-an-empty-set shape and is never a pass; 95 if no perturbable tensor exists at all, because a control that could not be applied is an absence and not a failure; only then 5 for a round trip that changed bits, 5 for a control that did not fire, and 0. The self-test drives that pure verdict function over synthetic payloads with no GPU, no model, no network and no filesystem, and passes 8 of 8 controls, including a MUST-FIRE for both RED branches, both 96 branches and the 95 branch, plus two controls that pin the ORDER so a key-set mismatch cannot be reported as a bit difference. Honest limitation, stated rather than buried: this is measured at adapter scale, 98 tensors and 1.51M elements, not at full-model scale. The comparator is the same code path at any size, but the row does not yet carry a reading over a 7.94B-parameter checkpoint, and re-saving one of those twice is tens of GB of estate I/O that has not been spent. |
| T1-5 | 10 steps + resume 10 == 20 steps straight (RESUME fidelity) | resume arm | straight-through arm; final weights must match within tolerance -- NOT RUN and not runnable: the declared control presupposes the capability it is meant to control for. What ran instead is a declarability control pair: a flag and a field that DO exist must pass the same two checks the unimplemented spellings fail, or a surface that rejected everything would score as correct strictness | ~40 min | ABSENT, not merely unproven — `train/loop.py` calls `trainer.train()` with no arguments and `resume_from_checkpoint` appears nowhere in `src/`, so no code path restores optimizer, RNG or dataloader state and the CLI declares no flag that could ask for one. The launch plane's `FS_RESUME_CKPT`/`FS_RESUME_STEP` vocabulary is real but stops at the container boundary (#122). Both arms have nothing to point at: the gap is in the framework, not in the measurement (#461) The absence has now been given a shape by a run rather than left as a reading (#486): on both surfaces a user can reach, this capability is refused BY NAME. Every probed command-line spelling comes back from argparse as an unrecognised option, and every probed TrainConfig field raises TypeError at construction, while a flag and a field that DO exist pass both checks -- so the rejection is about this capability and not about everything, which a surface that rejected its own real options would also have produced. The exit code is deliberately not the evidence: this CLI maps argparse's usage error onto the same 96 the plane uses for a designed refusal, so rc alone cannot separate 'no such flag' from 'refused by design', and the scoring key is the marker argparse prints, with rc recorded and never deciding. This does not move the row: the capability is still absent and this row still carries no reading of the claim above. It establishes only that the absence cannot be mistaken for a presence -- nobody can declare this and be silently ignored. |
| T1-6 | a checkpoint whose shard is a symlink to the base is CAUGHT | the #343 artifact | a real shard must pass the same detector | ~5 min | MEASURED GREEN (#479). Re-taken live rather than relabelled: the row read SHIPPED, which the measured countable skips, and the honest way out of that is a fresh reading, not a fresh adjective. Run from a pristine clone of main at 55a1bb6 with a clean worktree, against a real 12-shard 54,864,980,440-byte safetensors checkpoint -- deliberately a different artifact from the original two-shard ~51.6 GB one, so the verdict is not a property of a single index file. 1247 index entries read, the linked-shard arm RED as required, the dangling-shard arm RED as a LINK rather than as a missing file, zero link-following stats on shard paths, adjudicator self-test 11/11, exit 0 on both. The underlying fix (#343) is that is_symlink() is hoisted ahead of exists(), so a linked shard draws no link-following stat at all; the harness scores 8/11 pre-fix and the 3 that flip are the detector arms. The 8 that hold include two proving the src-layout import bootstrap both finds this checkout and declines a tree with no package, added after the launcher-contracts job showed the self-test assumed an installed distribution -- and that bootstrap is load-bearing: this row is two files, and shipping only the adjudicator leaves its sibling import to fail at module scope, before main() exists to classify anything into the contract. Re-measured through the documented runner path after #494 and #495. The checkpoint input had been a positional, which run_row.py cannot supply because it forwards only flags the adjudicator declares (#473), so this row was reproducible only by hand while reading MEASURED; --ckpt-dir now exists alongside the retained positional and two different values refuse 96 rather than resolve to either. Driven end to end: run_row.py --row T1-6 --flag ckpt-dir=<ckpt> returns 0 green over four arms. Proving that exposed #495 -- the arm writer assumed its --out-dir already existed, turning a missing directory into a 96 that reads as a harness fault -- which sixteen controls had missed because run_row's own fake adjudicator called os.makedirs while the real writer did not, a fixture kinder than the code it stands in for. Controls: t1_6 15 -> 17, run_row 16 -> 17, both new ones verified to fail when the fix is reverted. |
| T1-7 | DCP<->HF parity VERIFICATION: the framework can read a DCP checkpoint and a safetensors checkpoint through one interface and adjudicate truthfully whether they agree. The word 'converted' has been dropped from the original claim because no DCP->HF converter exists in this source; the verifier does, and only the verifier is claimed | compare a real FS-written safetensors checkpoint against a DCP checkpoint written from the same tensors -- NOT 'convert both ways', which cannot be run because there is no converter | four controls, not the one originally declared: one element moved by a single ULP (must be seen and graded CLOSE, not absorbed), the same element moved past the tolerance (must block and name the key, which is what makes the tolerance a ceiling rather than a rubber stamp), one tensor dropped (must fail as a one-sided key, a distinct mode from a numeric finding), and disjoint key sets (must be vacuous, never ok) | ~40 s measured, CPU only, no GPU and no download (the ~20 min estimate was wrong) | MEASURED GREEN (#485) on parity VERIFICATION, and it RETRACTS what this row used to say. The row read 'ABSENT', inferred from reading the source rather than from a run, and that is the third such inference this week to be withdrawn by measurement (after T1-18 and T1-19). The subject is not synthesised: the HF side is the safetensors checkpoint the training plane actually wrote during T1-19. The DCP side is written by torch's own writer, because this plane never emits DCP -- it refuses sharded execution (T1-14) -- and that asymmetry is the point, since the question is whether FoundationScale can read BOTH and tell the truth. It can. Identical inputs: 26 keys, 19743872 elements, every key graded bitwise-EXACT. One element moved by a single bf16 ULP: SEEN -- exactly one mismatched element, max absolute difference 0.0001220703125 -- and graded CLOSE, not EXACT, against the declared policy default-close (max_abs_diff<=0.01, cosine>=0.999, rel_frob<=0.01). That grade is the load-bearing detail: ok means 'within the declared tolerance', not 'identical', and because the grade is three-level rather than boolean, ok never hides a bitwise difference from a reader who looks. The same element moved by 1.0: graded DIFFER, blocks, names the key, so the tolerance is a ceiling that demonstrably can be exceeded rather than a policy that acquits everything. One tensor dropped: reported as a one-sided key, not as a numeric finding. Every key renamed: VACUOUS and not ok, so the all([]) guard this framework exists to hunt is live in the framework's own comparator rather than merely documented there. Adjudicated by t1_7_dcp_hf_parity_arms.py, 24 self-test controls, CLEAR on both the cluster's 3.11 interpreter and the laptop's 3.14. NOT established here: that FoundationScale can CONVERT between the formats -- it cannot, and the claim above was narrowed rather than left to be read as if it could; nor parity across a SHARDED DCP checkpoint with multiple write ranks, which no instrument can reach while the plane refuses sharded execution. |
| T1-8 | resharding: save on 4 GPUs, load on 2 | 4->2 | 4->3 (non-divisor) must REFUSE, not silently drop experts -- NOT RUN and not runnable: the declared control presupposes the capability it is meant to control for. What ran instead is a declarability control pair: a flag and a field that DO exist must pass the same two checks the unimplemented spellings fail, or a surface that rejected everything would score as correct strictness | ~30 min | ABSENT, and confirmed absent by a run rather than inferred from one: `reshard` appears in `src/` only as an error string in topology.py and in RL weight-transport docstrings, and no TrainConfig field or CLI flag exists through which a load-time parallel layout could be requested at all. Both declared arms have nothing to point at -- there is no 4->2 to run, and the 4->3 non-divisor control cannot refuse a request that cannot be made. The absence has now been given a shape by a run rather than left as a reading (#486): on both surfaces a user can reach, this capability is refused BY NAME. Every probed command-line spelling comes back from argparse as an unrecognised option, and every probed TrainConfig field raises TypeError at construction, while a flag and a field that DO exist pass both checks -- so the rejection is about this capability and not about everything, which a surface that rejected its own real options would also have produced. The exit code is deliberately not the evidence: this CLI maps argparse's usage error onto the same 96 the plane uses for a designed refusal, so rc alone cannot separate 'no such flag' from 'refused by design', and the scoring key is the marker argparse prints, with rc recorded and never deciding. This does not move the row: the capability is still absent and this row still carries no reading of the claim above. It establishes only that the absence cannot be mistaken for a presence -- nobody can declare this and be silently ignored. |

### T1 — optimizer / memory / kernels (GPU, each needs its T0 row first)

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T1-9 | adamw vs fused vs adafactor vs sgd differ in step time and loss curve | 4 arms, same seed | identical curves across optimizers = the knob did nothing | ~15 min x4 (est; measured 2 min 07 s total for all 4 arms through the harness) | MEASURED GREEN (#405/#406) on a GB200 tray: 4 optimizer arms (adamw, adamw_torch_fused, sgd, adafactor), 40 STEP-LABELLED loss points read from each arm's run_manifest.json; max abs delta 5.046e-01 > tol 1.0e-06, so the declared optimizer demonstrably moves the trajectory. Earned only after the #406 telemetry widening put a per-step train_loss_curve in the manifest and the reader was made step-keyed -- the pre-fix reader compared ONE aggregate per arm, so the same GREEN was unearned. RE-TAKEN at HEAD 8130ffb (#448 pass) and GREEN again: worst absolute delta 4.474e-01 against a 1.0e-06 tolerance, so the optimizer knob demonstrably moves the training at the commit that ships, not only at the ancestor it was first measured on. RE-TAKEN A THIRD TIME at HEAD 4a41e4d, and this one differs in kind: it was driven end-to-end by the shipped harness run_row.py rather than by hand, so the evidence is a receipt carrying the adjudicator's own sha256 (0243463941884c7c) and the claim's (f20aa9f4b2c3bb9e) beside the four arm files. GREEN again over adafactor, adamw, adamw_torch_fused and sgd, every arm departing from the identical first loss 4.050723075866699 and diverging after it. The same pass recorded telemetry the row had never stated: peak allocated memory separates the four optimizers monotonically and in the order optimizer state predicts -- sgd 7.18 GB, adafactor 9.99 GB, adamw_torch_fused 13.36 GB, adamw 15.51 GB -- so the fused arm is not merely different bookkeeping, it holds 2.15 GB less than plain adamw on the same model over the same steps. That is telemetry recorded under the claim, not a second claim adjudicated: the row's verdict still rests on the loss-curve differential. |
| T1-10 | accum=4,bs=1 and accum=1,bs=4 give the SAME loss curve | both arms | if they differ beyond tolerance, accumulation is wrong | ~15 min x2 (est; measured 1 min 21 s total for both arms through the harness) | MEASURED GREEN (#405/#406) on a GB200 tray: accum4_bs1 vs accum1_bs4 agree over the 40-step INTERSECTION of the two arms' step labels; worst gap 0.0133574 at accum1_bs4 step 7 (2.9078900814056396 vs 2.9212474822998047). Keyed on step, not on list position -- two arms logging at different cadences hold different steps at the same index, and a positional zip would have compared step-against-wrong-step and called it a verdict. RE-TAKEN at HEAD 8130ffb (#448 pass) and GREEN again: the two arms' curves agree within tolerance at the commit that ships, not only at the ancestor it was first measured on. RE-TAKEN A THIRD TIME at HEAD 4a41e4d through the shipped run_row.py harness rather than by hand, GREEN again over the same two arms, with a receipt carrying the adjudicator's sha256 (d6ea718a89da) and the claim's. The two curves agree to within roughly a hundredth across the pass (accum1_bs4 2.8327841758728027 against accum4_bs1 2.8310916423797607 at the first shared step), which is float non-determinism between two different batch shapes and not a disagreement about the update. HOLLOW AT THE SEAM (#475/#476): that harness pass wrote two arm files and neither carries a `status` key, so the receipt recorded two arms and no statement that either had ever been measured. The green was the all([]) trap one level down, and the same input REFUSES 96 under the fixed runner. The hand-taken readings above are unaffected and remain this row's evidence; what no longer stands is the claim that the harness reproduced them. Re-taking it through the harness needs the adjudicator to write an identity key -- it writes `launcher_exit` where the runner lifts `launcher_exit_code`, which is the #474 undeclared-convention defect in a second place. |
| T1-11 | grad checkpointing: peak memory DROPS and step time RISES | on vs off | neither moving = the flag is a no-op (the silent-fallback shape) | ~15 min x2 (est; measured 3 min 28 s total, 9 arm files over 3 interleaved rounds) | MEASURED GREEN on a GB200 tray at HEAD 8130ffb (#448 pass): 3 interleaved rounds, arm order rotated every round so a warm filesystem cache cannot be confounded with arm identity (#418). Peak memory DROPPED 30.8% in every round (18.008 GiB off to 12.457 GiB on, identical to three decimals across all three rounds) and step time ROSE in every round (throughput 2.397 to 1.841, 2.572 to 1.791, 2.512 to 1.877 steps/s -- +23.2%, +30.4%, +25.3%), each beyond the row's declared 5.00% tolerance with signs agreeing. The sensitivity probe is what makes the memory figure a measurement and not a constant: halving the batch peaked at 13.350 GiB, distinct from 18.008, so the instrument demonstrably sees the quantity it is being asked about. The #408 false RED does not reproduce. Adjudicator self-test 43/43. DRIVEN THROUGH THE HARNESS for the first time at 1460560, and HOLLOW AT THE SEAM in the same way (#475/#476): it exited 0 having written nine arm files, but every one of them carries `refused` where the runner lifts `status`, so the receipt would have recorded nine arms and no statement that any of them was measured. The arms are not empty -- they lift telemetry, excerpts and `launcher_exit_code` -- and that is what makes this hollow receipt hard to see: the evidence is present and only the identity is missing. The same input REFUSES 96 under the fixed runner. The #448 tray pass above was taken by hand, is unaffected, and remains this row's evidence until the adjudicator writes an identity key the runner knows. |
| T1-12 | eager/sdpa/flash agree on logits within tolerance, differ in step time | 3 arms | identical step times = the impl never changed | ~10 min x3 (est; measured 3 min 21 s total for all 3 arms, one of which refused) | UNMEASURED 95 (#405/#406) on a GB200 tray: eager and sdpa each returned a real 40-point step-labelled loss series, but flash_attention_2 is UNMEASURED -- ModuleNotFoundError: No module named 'flash_attn'. Deciding metrics exist for 2/3 arms, so the control rule cannot be evaluated and the row correctly refuses rather than reporting a 2-arm result as the 3-arm claim. SEPARATELY, AND NOT A CHALLENGE TO THAT VERDICT (#493): this row's adjudicator is the third of three whose evidence the runner's receipt lift cannot see. It is not a misnaming like T1-10's `launcher_exit` or T1-11's boolean `refused` -- it writes the series at `metrics.loss_series.value` as `[step, loss]` pairs, while the lift reads a flat `loss_curve` of `{step, loss}` objects. Name, nesting and element shape all differ, so a receipt for this row would carry no curve and be indistinguishable from a receipt for a row that never produced one. The 2-of-3 verdict above was taken by hand and does not depend on the lift; what is recorded here is that the harness cannot currently carry this row's evidence into a receipt. Conversion is deferred behind the namespaced writer. |
| T1-13 | CPU offload: optimizer state leaves GPU memory | offload on vs off | the SAME flag at --cpu-optimizer-offload false must train to completion and write weights; if it also refuses, the flag's presence is the trigger and the refusal says nothing about the declared value | ~20 min x2 | MEASURED GREEN on the refusal, and the capability is ABSENT (#480/#373). This row's claim -- that optimizer state leaves GPU memory -- is NOT satisfied by this framework and nothing here shows that it is; the declared memory-differential control cannot fire, because there is no offload arm to weigh. What is proven is the narrower and previously unproven thing: declaring cpu_optimizer_offload=true is refused at the optimizer-offload site itself, the message echoes the declared value, the run trains nothing, writes ZERO checkpoint artifacts and records stage=refused, and it still refuses when the model cannot resolve -- so the refusal precedes model load. The same flag at false trains to completion and writes 7 artifacts on identical fixtures, which closes the explanation that the plane dies whenever this flag is present at all. Measured on a tray against a pristine clone of main at 55a1bb6, adjudicated by axis_refusal_arms.py, whose thirty-eight self-test controls pin the verdict order and are exercised across all three axes. Exit codes could not have settled this: the plane reaches 96 from an argument-parser error, from the config validator, and from four separate refusal sites inside the training loop, so each arm is scored on WHICH site spoke and an arm carrying another axis's marker is a failure rather than a pass. Eleven arms in total; the two shared controls are a no-axis baseline that trains and writes 7 checkpoint artifacts, and the same baseline against an unresolvable model, which exits 5 -- so an unresolvable model is otherwise fatal, which is what makes the ordering arm below evidence of sequence rather than of indifference. |
| T1-14 | ZeRO-1/2/3 reduce per-rank memory monotonically | 3 arms + DDP baseline | the SAME flag at --sharding-strategy ddp must train to completion and write weights; if it also refuses, the flag's presence is the trigger and the refusal says nothing about the declared value | ~30 min x4 | MEASURED GREEN on the refusal, and the capability is ABSENT (#481/#373). This row's claim -- that ZeRO-1/2/3 reduce per-rank memory monotonically -- is NOT satisfied by this framework and nothing here shows that it is; the declared flat-memory control cannot fire, because there are no ZeRO stages to compare. What is proven is that declaring sharding_strategy='zero3' is refused at the sharded-execution site itself, the message echoes the declared value, the run trains nothing, writes ZERO checkpoint artifacts and records stage=refused, and it still refuses when the model cannot resolve. The same flag at 'ddp' -- the one strategy with execution behind it -- trains to completion and writes 7 artifacts on identical fixtures, so the refusal tracks the VALUE and not the presence of the declaration. Measured on a tray against a pristine clone of main at 55a1bb6, adjudicated by axis_refusal_arms.py, whose thirty-eight self-test controls pin the verdict order and are exercised across all three axes. Exit codes could not have settled this: the plane reaches 96 from an argument-parser error, from the config validator, and from four separate refusal sites inside the training loop, so each arm is scored on WHICH site spoke and an arm carrying another axis's marker is a failure rather than a pass. Eleven arms in total; the two shared controls are a no-axis baseline that trains and writes 7 checkpoint artifacts, and the same baseline against an unresolvable model, which exits 5 -- so an unresolvable model is otherwise fatal, which is what makes the ordering arm below evidence of sequence rather than of indifference. |

### T1 — architecture (GPU)

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T1-15 | a dense model trains end-to-end and the bytes move | Qwen dense, lr>0 | an `lr=0` arm through the same path: weight parity must show ZERO movement | ~15 min x2 | done (#162/#176, #366) — control arm RUN on a GB200 tray: the lr>0 arm moved 323/338 tensors against an `lr=0` arm at step 20, and that same `lr=0` arm moved 0/338 between its own step 10 and step 20, so the movement is attributable to training; the 15 unmoved tensors are 1-D norm weights whose 20-step update falls below half a bf16 ULP, not a frozen submodule (#462) |
| T1-16 | an MoE model trains and experts receive gradient | MoE arm | a router that never routes = zero grad on some experts; must be detected | ~40 min | RUNTIME ARM RUN, and the defect that blocked adjudication is now FIXED and re-measured; the row is still not green and the reason for that has changed. MEASURED on a GB200 tray: a real 26B-A4B MoE (128 experts over 30 layers) trains end-to-end through the shipped CLI -- LoRA rank 8 on thirty language layers, ag_news, 10 steps, rc 0, checkpoint written and the save gates ran. So "an MoE model trains" is established. When this row was first written the second half could not be adjudicated at all, because the framework declared the model DENSE and both expert gates skipped as "0 units (dense model)" on a model with 128 of them. That is #471, and #498 is its root cause: the config reader was a flat top-level getattr while every MoE on this estate nests the count under text_config (Gemma4Config 128; Qwen3_5MoeConfig 256 on both 35B-A3B checkpoints, so it is the transformers 5.x convention and not a multimodal quirk). Both are fixed. Re-MEASURED against the artifacts this arm itself produced, reading the safetensors header only: the flat read returns None where the tree read returns text_config.num_experts=128; the declaration is now UNKNOWN -- config declares 128 experts but 0 of 110 tensors are expert-named, the two sources disagree, so neither is adopted -- and both expert gates return a named abstention, "adapter-only checkpoint: all 110 of 110 saved tensor name(s) are adapter-namespaced", which states in words that it is NOT a claim the model is dense. The earlier note that searching text_config first sends every LoRA run over an MoE to a false RED no longer describes the behaviour: that false RED WAS the missing adapter-scope abstention, which is now implemented and carries a control proving a GUTTED checkpoint still blocks. What remains is a limit of the arm, not a defect: under LoRA the expert weights are never in the artifact, so "experts receive gradient" cannot be established here in principle and the honest verdict is the abstention. Establishing it needs a full-weight MoE arm, which is a different and far more expensive run. The control arm (a router that never routes) is still unrun, and is no longer blocked. Also measured on the way in: the bare suffix q_proj reaches the VISION tower on this family, where it is a Gemma4ClippableLinear that peft refuses, so the first attempt died before step 1 and adapter targets must be spelled language-scoped. ADAPTER SCOPE IS NOW THE FRAMEWORK'S JOB RATHER THAN THE OPERATOR'S (#522). That sentence recorded a real measurement and then prescribed a workaround the operator had to remember, which is the weaker half of a fix; it is superseded here rather than deleted, because the measurement it rests on is still the evidence. FoundationScale now resolves the model family from model_type and scopes adapter targets to that family's language tower before peft is constructed, handing peft fully-qualified module names. The mechanism is peft's own: a list target_modules is matched with key.endswith(target), so a qualified name selects exactly one module, and tower scoping therefore needs no regex, no exclude_modules and no peft version bump -- it needs the SELECTOR to stop emitting bare leaf names. The declaration was never wrong; q_proj is a reasonable thing to adapt. MEASURED as a controlled A/B on a GB200 tray: same tray, same container image, same launcher, same STEPS=12 BS=1 SEQ=2048 NPROC=4, and the SAME bare-leaf declaration --adapter-target q_proj k_proj v_proj o_proj, with the staged src tree as the only variable. gemma-4-26B-A4B went from rc 1 at 20 s having run 0 steps to rc 0 with 12 of 12 steps, train_loss 2.545 at 9,526 tokens/s; gemma-4-31B from rc 1 at 20 s having run 0 steps to rc 0 with 12 of 12 steps, train_loss 1.716 at 5,390 tokens/s. Pre-fix both died on all 4 ranks with the peft ValueError naming Gemma4ClippableLinear at in_features=1152, which is the vision-tower width and not the language width; post-fix that string occurs 0 times in either log. The exclusion is ANNOUNCED and not merely avoided -- 'EXCLUDED 108 module(s) under model.vision_tower (Gemma4ClippableLinearx108)' printed against 'selected 115' and 'selected 230 module(s) under model.language_model' -- because a framework that quietly declines to adapt a tower is indistinguishable from one that forgot it existed, and the whole defect above was legible only after the fact. Evidence: fs-probes/perf_ladder/r11_family_plane_ab_2026-09-20.json. One caveat on the rc: torchrun collapses the four-state exit contract, so the pre-fix exitcode 5 reaches the shell as rc 1 -- the train.log, not the rc, is authoritative here. A SECOND DEFECT IN THE SAME CLAIM, FOUND BECAUSE THE FIRST ONE WAS FIXED (#524). Scoping adapters to the language tower is necessary and not sufficient: it says WHERE an adapter may go, never HOW MUCH of the depth one actually reached. Measured against the three real Qwen3.5 checkpoints with the same conventional declaration --adapter-target q_proj k_proj v_proj o_proj: 27B selected 64 modules over 64 layers, 35B-A3B 40 over 40, 122B-A10B 48 over 48 -- that is selected == full_attention * 4 exactly on all three rows, because Qwen3.5 is a 3:1 hybrid in which only full_attention layers carry self_attn.{q,k,v,o}_proj and the other three quarters carry linear_attn.{in_proj_qkv,in_proj_a,in_proj_b,in_proj_z,out_proj} -- note the linear-attention output projection is spelled out_proj, not o_proj, so no declared leaf name can reach it by accident. The run returned rc 0, the loss fell, the checkpoint saved, and one quarter of the network's depth was adapted with nothing saying so. The existing announcement could not catch this: it reports a MODULE count, and 64 modules spread over 16 layer positions is indistinguishable from 64 spread over 64. The information was already in data the selector held and was simply not derived. It is derived now, from the shape of the qualified names alone -- a layer position is the prefix up to and including the FIRST integer segment, so model.layers.3.mlp.experts.7.gate_proj belongs to position 3 and not 7 and an MoE expert index cannot inflate a depth denominator. A position counts against the declaration only if it holds something the adapter predicate would have accepted, so a pure norm stack is not charged as a gap it was never possible to close. The line is emitted ALWAYS, including at full coverage, because in this repository the difference between measured-and-complete and never-measured is load-bearing, and a prefix with no integer-indexed positions reports UNMEASURABLE rather than a vacuous 0 of 0. This ANNOUNCES and does not refuse: partial-depth LoRA is a legitimate thing to want, an unannounced one is not. No family knowledge was added to do it -- the arithmetic comes from module paths, so a family that registers tomorrow gets depth coverage without another edit, which is the same property the registry exists to hold. #499 is open on the same code path and is published as open: train/loop.py reads five expert-count key names while the provenance producer and the probe read three, so a model declaring one of the two extra names would be MoE to the trainer and dense to the record. No model measured here uses those names; it is pinned by controls that freeze both lists, and unifying them would invalidate rows measured against the narrow list. CONTROL ARM RUN, AND IT FOUND ITS OWN INSTRUMENT MISSING (#500). The declared control -- a router that never routes, so some experts take zero gradient -- was executed for the first time as a mechanism study on a tiny FULL-WEIGHT Qwen2-MoE (4 experts, 2 MoE layers, 181,440 parameters, 30 AdamW steps), the two arms differing ONLY by a forward hook that masks experts 2 and 3 out of the router's top-k; seed, init, data, optimiser and step count are identical, so any difference is attributable to routing alone. The hook does what it claims: the step-0 per-expert max absolute gradient in the control arm is e0=4.252e-03, e1=2.916e-03, e2=0.000e+00, e3=0.000e+00, against e2=2.167e-03 in the run arm. But at the AdamW DEFAULT weight_decay=0.01 the zero-gradient experts still MOVED, by 2.166e-04 and 2.294e-04 against 2.19e-01 for the routed ones, and the count of bitwise-frozen experts was zero -- so an exact-equality movement probe reports a never-routed expert as trained. Re-run at weight_decay=0.0 the same two experts are bitwise EXACT at 0.000e+00 and the frozen set is exactly (2, 3), which is the control proving decoupled weight decay is the cause: AdamW decays every parameter whether or not it has a gradient. The separation is still about 1000x in magnitude, so a THRESHOLD instrument works and an exact-equality one does not. Second half, and the reason this row cannot simply adopt the shipped parity instrument: parity is keyed per FQN, and the dominant HF MoE layout stacks every expert of a layer into ONE tensor (`model.layers.0.mlp.experts.gate_up_proj`, shape (4, 128, 64)), so parity returns DIFFER for BOTH arms identically and cannot see the dead expert at any weight-decay setting. That is ExpertDistinctnessGate's own documented stacked-layout blind spot, now measured at the data level as well as the metadata level. So the control arm is no longer unrun: it ran, it is reproducible, and it establishes that no shipped FoundationScale instrument detects the condition this row says must be detected. What the row still needs is a full-weight MoE driven through the shipped entry point plus a per-expert-slice, threshold-based probe; #500 is published open rather than fixed on release eve, because adding an instrument is a new claim and this row has no budget left to measure one. STAGE C RUN THROUGH THE SHIPPED ENTRY POINT, AND IT CORRECTS TWO SENTENCES OF #500 (#501). The follow-up #500 said this row still needed -- a full-weight MoE driven through the shipped entry point plus a per-expert-slice probe -- has now been run. Fixture: a REAL-architecture Gemma4 MoE, every structural knob inherited from the estate's shipped gemma-4-26B-A4B config (the same Gemma4TextConfig class, the same Gemma4TextRouter, enable_moe_block=true, the same stacked expert layout) and only the sizes shrunk, to 2 layers, 8 experts, top-k 2, 70,198,672 parameters. Both arms are `python -m foundationscale.train.cli` on ag_news, full-weight, 30 steps, seed 42, precision bf16 declared, byte-identical command lines; the ONLY difference is an environment variable read by a shim that writes -inf into the router's own expert_scores before its softmax, so experts 2 and 3 are never routed. POSITIVE CONTROL: in the run arm experts 2 and 3 received 936 and 771 routed tokens; in the control arm they received 0 and 0, and the other six absorbed the traffic. FIRST CORRECTION TO #500: #500 recorded that decoupled weight decay moves a zero-gradient expert and defeats an exact-equality probe. That holds for the hand-rolled torch.optim.AdamW #500 used, whose default weight_decay is 0.01. It does NOT apply to this framework's own path: FoundationScale never sets weight decay and inherits transformers TrainingArguments, so the arms above ran at weight_decay=0.0 under adamw_torch_fused, read back from the saved training_args. On the shipped path an exact-equality movement probe is therefore sound, and #500's warning should be read as scoped to an optimiser FoundationScale does not construct. RESULT OF THE PER-SLICE PROBE: in the control arm all four stacked expert tensors give exactly 0.000e+00 movement at slices 2 and 3 and the bitwise-frozen set is exactly (2, 3), while the six live experts move by about 6e-04, which is the bf16 resolution at that magnitude; in the run arm no slice is frozen at all, so the probe does not false-positive. The remedy #500 proposed works, and on this path it needs no threshold. WHAT PARITY SEES: the shipped per-FQN comparator returns DIFFER for all four stacked tensors in BOTH arms identically, so #500's second half is confirmed on a real architecture through the real entry point rather than only on a toy. SECOND AND MORE IMPORTANT CORRECTION TO #500: #500 concluded that nothing FoundationScale ships detects the condition this row says must be detected. Nothing detects it, but that sentence understates the framework and is withdrawn as written. Run against these checkpoints, the shipped checkpoint.expert_distinctness gate does not pass and does not stay silent: it returns SKIP with abstention NOT_ESTABLISHED, names the stacked layout, reports that all four leading dims equal the declared 8 experts and that every metadata property it CAN check was checked and showed nothing wrong, states that per-expert identity inside a stacked tensor is unobservable from metadata by construction, and names the remedy in its own words -- a data-level per-slice comparison that hashes every expert slice within each stacked tensor, which is precisely the probe run above. Because NOT_ESTABLISHED stays inside the denominator, checkpoint.first_save then reports UNDERCOVERED at 2 of 3 sub-gates and stops the run, rc 5, in both arms. So the honest statement is that FoundationScale cannot see a never-routed expert and refuses to certify the checkpoint instead of passing it, which is the doctrine working rather than failing. NEGATIVE RESULT worth recording so the cheap shortcut is not attempted: router.per_expert_scale is the one genuinely per-expert-indexed 1-D tensor in this architecture, and it is bitwise frozen for ALL EIGHT experts in BOTH arms, as is router.scale, so it carries no signal and cannot serve as a dead-expert detector. SCALE CHECK on the real artifact rather than the fixture: the shipped gemma-4-26B-A4B stacks 128 experts per layer over 30 layers, so 3,840 expert MLPs sit behind 60 per-FQN keys, and the same probe run there resolved num_experts=128 from text_config with the artifact census corroborating, against 0 for its dense 31B sibling. #501 is filed, not fixed: the per-slice hash is now measured and prescribed by the gate itself, but adding it as a shipped instrument is a new claim. |
| T1-17 | ep=2 shards experts across ranks | ep=2 vs ep=1 | the SAME flag at --ep 1 must train to completion and write weights; if it also refuses, the flag's presence is the trigger and the refusal says nothing about the declared degree | ~40 min | MEASURED GREEN on the refusal, and the capability is ABSENT (#482/#375). This row's claim -- that ep=2 shards experts across ranks -- is NOT satisfied by this framework and nothing here shows that it is; the declared per-rank-memory control cannot fire, because there is no expert-sharded arm to weigh. What is proven is that declaring ep=2 is refused at the unwired-degree site itself, naming the degree it refused, training nothing, writing ZERO checkpoint artifacts and recording stage=refused, and that it still refuses when the model cannot resolve. The same flag at 1 trains to completion and writes 7 artifacts on identical fixtures. A topology confound was possible here and did not occur: ep=2 on a single GPU declares a parallel product larger than the world, and had a topology validator rejected it first the arm would have shown the validator's marker and reached no refusal site at all -- the adjudicator refuses to call that GREEN, and it did not have to. Measured on a tray against a pristine clone of main at 55a1bb6, adjudicated by axis_refusal_arms.py, whose thirty-eight self-test controls pin the verdict order and are exercised across all three axes. Exit codes could not have settled this: the plane reaches 96 from an argument-parser error, from the config validator, and from four separate refusal sites inside the training loop, so each arm is scored on WHICH site spoke and an arm carrying another axis's marker is a failure rather than a pass. Eleven arms in total; the two shared controls are a no-axis baseline that trains and writes 7 checkpoint artifacts, and the same baseline against an unresolvable model, which exits 5 -- so an unresolvable model is otherwise fatal, which is what makes the ordering arm below evidence of sequence rather than of indifference. |
| T1-18 | LoRA through the package: adapter weights train, base frozen | LoRA arm | base-weight parity must show ZERO movement; adapter parity must show movement | ~2 min measured (68 s for all three arms on one GPU; the ~20 min estimate was wrong) | MEASURED GREEN (#483), and it RETRACTS what this row used to say. The row read 'ABSENT from the package plane (#329)', inferred from reading the source rather than from a run, and that is wrong: TrainConfig carries adapter, adapter_rank, adapter_alpha, adapter_targets and adapter_dropout, train is a public callable, and a config built by hand in Python -- no command line anywhere -- attaches peft and trains the adapter. Three arms on a tray against a pristine clone of main at 55a1bb6. The LoRA arm moved 112 of 112 lora_B tensors, max Frobenius norm 0.209401; the lr=0 null arm left all 112 at exactly 0.0, a control that cannot pass by chance because lora_B is zero-initialised and so needs no threshold to calibrate. The base is frozen, and that is shown twice by independent means: the saved artifact carries 0 base tensors, while the full-fine-tune control writes 676 of them and moves 12 of 12 sampled against the pretrained weights -- so the base-movement detector is demonstrably able to fire, and 'no movement' here is a reading rather than an untested emptiness -- and separately the manifest records 1089536 of 1544803840 parameters trainable, 0.0705%. The declaration is recorded, not merely honoured: the manifest carries adapter=lora, rank 8, alpha 16.0, targets declared q_proj,v_proj resolved to 56 real module paths, and declaration.adapter_count_cross_check='agree (112)', which is not a rubber stamp because the full-fine-tune arm records '(not applicable: no peft wrapper)' instead. So this is NOT the accepted-and-silently-dropped class of #375. Adjudicated by t1_18_package_lora_arms.py, 25 self-test controls, CLEAR on both the cluster's 3.11 interpreter and the laptop's 3.14; exit 0 on the self-test and on the real payload. The one thing NOT established: this is one adapter mode on one model family, and the CLI path (T1-20) remains the separately measured one. |
| T1-19 | tied embeddings: tying survives save/load. The 'and sharding' half of the original claim is unreachable on this plane, which refuses sharded execution (T1-14), and has been dropped from the claim rather than silently counted as passing | a tied-embedding model | untied model as control -- synthesised, not borrowed: no untied model with real weights exists on this estate and downloading is not permitted, so both arms are built from one config differing in exactly one field, tie_word_embeddings | ~68 s measured (33.6 s tied + 34.1 s untied on one GPU; the ~20 min estimate was wrong) | MEASURED GREEN (#484) on the save/load conjunct, and it RETRACTS what this row used to say. The row read 'ABSENT (#172/#202 shape)', inferred from reading the source rather than from a run, and that is wrong. Two arms on a tray against a pristine clone of main at 55a1bb6, built from one config differing in exactly one field. The deciding check is STORAGE IDENTITY after a round trip, not equality: a model that saved two copies of one tensor and reloaded them into two buffers is byte-identical to a correctly tied one at rest and broken in motion, because the next update to the embedding would not reach the head. The tied arm reloaded with output and input embeddings sharing storage, lm_head absent from the artifact (26 tensors against the control's 27, the delta being exactly lm_head.weight), config recording tie_word_embeddings=true, after the embedding actually moved -- max absolute delta 0.003174, so this is a TRAINED tie and not an untouched one. The untied control wrote its own lm_head and reloaded with separate, unequal storage, which is what proves the detector can report a mismatch; without it, storage identity on the tied arm would be consistent with a detector that answers yes unconditionally. Parameter counts corroborate the arms really differed: 19743872 tied against 39191680 untied, a difference of 19447808, which is exactly one embedding matrix. Adjudicated by t1_19_tied_embed_arms.py, 26 self-test controls, CLEAR on both the cluster's 3.11 interpreter and the laptop's 3.14. NOT established here: the sharding conjunct, which no instrument can reach while the plane refuses sharded execution, and behaviour on a pretrained checkpoint at scale, since the subject was a small model built from config -- though the save path does not know the difference, and a real 1.5B tied checkpoint was separately observed to keep lm_head absent through the same path. |

### T1 — modality (GPU) — the stated core purpose

| ID | Claim | Run arm | Control arm (must differ) | Cost | Status today |
|---|---|---|---|---|---|
| T1-20 | text: a text corpus trains end-to-end and the bytes move | text arm, lr>0 | an `lr=0` arm through the same path: weight parity must show ZERO movement | ~15 min x2 | MEASURED and GREEN on a GB200 tray with a control that is EXACT rather than tolerance-based. Two arms differing in ONE axis -- the learning rate -- over the same script, model, corpus, step count and seed 42: Qwen2.5-1.5B on ag_news, 20 steps, LoRA rank 8 targeting q_proj and v_proj, run arm lr=5e-5 and null arm lr=0. lora_B is zero-initialised and every AdamW update term is scaled by the learning rate, so the null arm admits NO epsilon. Measured: the run arm moved 56 of 56 lora_B tensors (Frobenius norms 0.0080 to 0.0204) and the lr=0 arm left every one of the same 56 at precisely 0.0. The null arm ALONE cannot tell 'lr=0 froze the weights' apart from 'saving always writes zeros'; the run arm is what excludes that, being the identical code path with one axis changed and a nonzero save. The shipped adjudicator carrying its own self-test landed at cb5e6c5 (#470), and the row was then RE-TAKEN end-to-end through the harness run_row.py at e1b32e2 rather than by hand: verdict green, pass t1_20_20260916_043424, receipt carrying adjudicator_sha256 5f728ca4dd0a14ce and claim_sha256 f2edcc4b8308aadb, interpreter Python 3.11.15 on aarch64. The two arms were read from payload FILES the adjudicator wrote, never scraped from stdout: the run arm 56 of 56 lora_B tensors moved with norms 0.008017275482416153 to 0.020371997728943825, the null arm 56 declared and 0 moved with a maximum norm of exactly 0.0. That reproduces the hand-taken numbers above through a different route. The pass cost two defects in the instrument on the way, both found by running it and both fixed with MUST-FIRE controls: #473, the runner collapsed a repeated --flag so it would have adapted v_proj alone while reporting the whole claim, and #474, this adjudicator printed its payload and wrote no arm files, so a genuine green came back through the harness as REFUSE 96 and the measurement was discarded at the seam. Prior history: #162/#176, #366 established the run arm only. Re-taken at full fine-tune scale on 2026-09-18: gemma-4-E4B-it was fully fine-tuned, no adapter, in bf16, on 2 GPUs of one GB200 tray under torchrun DDP, for 600 steps at per-device batch size 1 with gradient accumulation 4, learning rate 1e-5 cosine, 40 warmup steps, seed 42, checkpoints every 150 steps; the run exited 0 with the framework's own done-marker and loss moved 5.471 -> 1.034. Without gradient checkpointing this arm peaked at 86 GB per GPU. The bytes were measured moving directly on the saved safetensors rather than inferred from the loss: language moved 464 of 667 tensors, with worst absolute delta 1.869202e-03 at checkpoint-150 and 3.830910e-03 at the final checkpoint, and the growth between the two checkpoints is the tell that separates continued training from numerical noise. The same probe read two further buckets on the same checkpoints and found vision 0 of 658 and audio 0 of 751, both exactly 0.000000e+00, at both checkpoints; gemma-4-E4B-it is an OMNI checkpoint carrying vision and audio towers that this run's declaration does not exercise, the run announces them as carried-untrained, and these two zeros confirm that announcement is true rather than merely printed. Not established: 600 steps is about 22 percent of one pass over the corpus, so no claim is made about convergence or final model quality. |
| T1-21 | an image-bearing corpus trains and the vision tower's weights MOVE | image arm | a text-only arm through the same path: tower weights must NOT move | ~30 min | MEASURED and GREEN on a GB200 tray, with a control that could have fired and did not. History: the declared-image arm was PROCESSOR-FAMILY DEPENDENT and unreachable on Gemma-4 for three independent causes (#448, follow-up to #371) -- a flat image list that normalises to ONE image-batch so N images and N texts read as 1 vs N, raw row text carrying no chat template and so no placeholder token for the pixels to land on, and a constant TOKENIZE_MAX_LENGTH smaller than the prompt surface's OWN recorded tokens per image. #449 fixed the shape on BOTH planes that write it -- the train collator and the RL rollout encode path -- by nesting one image sub-list per feature, including an EMPTY sub-list for a text-only row, and by building text through the processor's OWN apply_chat_template so no family's placeholder is hardcoded. #450 then fixed the objective that a GREEN run had hidden: labels were input_ids.clone(), and MEASURED over four corpus rows 73.6 percent of label positions were the image placeholder id, whose input embedding is replaced by vision-encoder output, so emitting it as text is never a valid target. That run reported train_loss 19.16 against ln(vocab) 12.477, worse than uniform random; masking placeholders and padding took the identical command to 3.845. Both families now collate through the SHIPPED code with zero placeholders and zero pad slots left unmasked and a non-vacuous supervised count: gemma-4-E4B-it resolves placeholder id 258880, qwen2.5-vl-32b resolves 151655 from a PROCESSOR-ONLY attribute that a tokenizer-first order returns None for. The row's own claim is now measured directly rather than deferred. A full fine-tune cannot watch the tower move here, 7.94B parameters of weights plus fp32 Adam state against roughly 35 GiB of free device memory, so the arms use LoRA as an exact instrument: lora_B is zero-initialised and AdamW skips a parameter whose grad is None, so a nonzero norm is movement with no tolerance to argue about. Both arms attach the SAME adapter to the SAME 49 modules -- 48 matched by suffixes that exist only inside the vision tower, plus ONE language module that is not under test and is the control's control, because a text-only arm whose vision LoRA is still zero proves nothing if that arm never trained at all. Ten steps each on gemma-4-E4B-it, gradient checkpointing OFF because pixel values never require grad and a checkpointed vision segment can silently yield none, both arms rc 0: the image arm moved 48 of 48 vision-tower modules at a worst absolute B norm of 1.7055e-02, and the text-only arm, the same rows with the image column stripped, moved 0 of 48 while still moving its language anchor 1 of 1. So the control arm demonstrably trained and the tower still did not move. Run three times -- twice ad hoc and once through the shipped adjudicator on bytes md5-matched to the committed blob. The 48-of-48 against 0-of-48 split is identical every time because it is a bit-exact question; the worst norm moves in the third decimal (1.7105e-02, 1.7084e-02, 1.7055e-02) because lora_A is randomly initialised per run, which is also how one can tell the number was re-measured rather than re-read. The row now carries an adjudicator rather than prose: t1_21_vision_tower_arms.py runs both arms, classifies each adapted module by the same suffix rule peft used to select it, and decides from the norms alone. Its --self-test needs no GPU, no model and no network and drives the real adjudication function over synthetic norms -- 15 of 15 controls PASS, with a MUST-FIRE control behind every RED, 95 and 96 branch, including the one that matters most: an unmoved tower in a control arm that moved NOTHING AT ALL is reported 95, never GREEN, because that arm never trained. Found en route and fixed as a precondition (#451): the train CLI's only repeatable flag, --adapter-target, raised AttributeError inside argparse on EVERY use, because the declared-fields probe rewrites every default to a sentinel object and argparse's append action calls .append on a copy of it, so the flag could not be used at all. Re-taken at full scale on 2026-09-18: the LoRA instrument quoted above was exact under the roughly 35 GiB memory budget it was written for, and this run had a whole GB200 tray, so that constraint no longer binds and the tower was watched directly. gemma-4-E4B-it was full fine-tuned, no adapter, bf16, on 2 GPUs of one GB200 tray under torchrun DDP, 184.3 GiB of device memory per GPU, for 600 steps at per-device batch size 1, gradient accumulation 4, learning rate 1e-5 cosine, 40 warmup steps, seed 42, checkpointing every 150 steps. The arms differed in the image declaration: the image arm declared the image column, and the text arm was the same 21,494 rows with the image key popped out. They differed in a SECOND axis too, disclosed here rather than left to be discovered: the image arm ran with gradient checkpointing and the text arm without it, because without it the image arm does not fit at all. Gradient checkpointing changes what is recomputed during the backward pass, not which parameters receive gradient, so it does not account for a vision tower that moved 149 tensors in one arm and 0 in the other -- but the arms are not single-axis and this row will not claim they are. The corpus was pre-filtered to images that actually decode; a fifth of the source corpus does not decode while existence checks pass on all of them, so the filter was on decode, not on presence. Both arms exited 0 with the framework's own done-marker; image arm loss went 2.777 -> 0.8221 and text arm loss went 5.471 -> 1.034. Memory was measured, not assumed: the text-only full fine-tune without gradient checkpointing peaked at 86 GB, the image arm without gradient checkpointing went OOM trying to allocate 8.54 GiB with 179.6 GiB already in use of 184.3 GiB, and with gradient checkpointing on the image arm peaked at about 116 GB and trained. Movement was measured directly on the saved safetensors, base versus checkpoint, per tensor, bucketed into vision, language, and audio by parameter name, not through an adapter proxy. At checkpoint-150, vision moved 150 of 658 tensors with worst absolute delta 8.544922e-04, language moved 463 of 667 with worst 1.876831e-03, and audio moved 0 of 751 with exactly 0.0. At the final checkpoint, vision moved 149 of 658 with worst 1.436234e-03, language moved 463 of 667 with worst 3.473282e-03, and audio moved 0 of 751 with exactly 0.0. The worst deltas grew between the two checkpoints in both vision and language, which was the tell that separated continued training from numerical noise, since noise would not have grown monotonically. One inconvenient fact is disclosed rather than omitted: the vision count went from 150 to 149, meaning one vision tensor that registered a nonzero delta at step 150 was bit-identical to the base at the final checkpoint; no cause was measured and none is claimed. The audio result was an unplanned negative control: gemma-4-E4B-it is an omni checkpoint carrying text, vision, and audio towers, this training plane declared audio untrainable, and the run announced which towers it was carrying untrained, so audio sitting at exactly 0 of 751 at both checkpoints independently confirmed that the announcement was true rather than merely printed, and confirmed it was not masking a tower that quietly received gradient. A blocker was found and fixed en route, #504: the plane hardcoded ddp_find_unused_parameters to False while declaring audio untrainable, so the audio tower could never receive gradient and DDP aborted every multi-rank run on the first backward pass; the first ungradiented parameter index DDP named was exactly 833, which was precisely where the audio tower began. The fix derived the flag from the run's declaration and the loaded module tree instead of hardcoding it, and False remained the default so that a genuinely unused parameter on a single-tower model would still be reported. Not established: 600 steps was epoch 0.2233, about 22 percent of one pass over the corpus, so no claim is made about convergence or about final model quality; the row's claim was that the tower moves under a real image-bearing corpus, and that is what was measured. The control arm was then measured at the same scale: the same 21,494 rows with the image key popped out, the same model, the same 600 steps and the same seed, differing from the image arm in the image declaration and -- as disclosed above -- in gradient checkpointing, which the image arm requires in order to fit and which governs recomputation rather than which parameters receive gradient. On the control arm's saved safetensors, base versus checkpoint, the same per-tensor probe found vision moved 0 of 658 tensors with worst absolute delta exactly 0.000000e+00 at both checkpoint-150 and the final checkpoint, and audio likewise 0 of 751, exactly 0.0, at both. The control arm demonstrably trained, which is what stops the zero from being vacuous: language moved 464 of 667 tensors, with worst absolute delta 1.869202e-03 at checkpoint-150 growing to 3.830910e-03 at the final checkpoint; a frozen-everything arm would have shown zero there too and would have proved nothing. The discriminator is therefore bit-exact and needs no tolerance: 149 of 658 vision tensors moved with the image column declared and 0 of 658 moved without it, over the same rows. Because the probe is written to assert vision movement, it exited 5, RED, on this arm; this is the control firing as designed, not a failure. One inconvenient fact is disclosed: the language bucket moved 464 of 667 tensors on this text-only arm but 463 of 667 on the image arm, a one-tensor difference for which no cause was measured and none is claimed. |
| T1-22 | declaring video REFUSES cleanly today | video corpus; and after #490, seven arms on a tray -- three RL (declared, absent, wrong-type) and four SFT (video-only, an undeclared text+video key, declared video, image control) | must be 96, not a silent text-only train; the undeclared arm must TRAIN with its drop NAMED and scores RED if it refuses, because refusing a corpus for carrying a key called video is name-sniffing; and #477 stands as the pre-fix must-fire, where the same deciding corpus exited 0 and named the modality in neither its output nor 30,671 characters of manifest | ~2 min on 1 GPU for the seven arms; each arm must see exactly ONE device -- with two visible, this single-process run wraps in DataParallel and wedges silently (observed at 25 min with 0% GPU and one pegged core, #492) | MEASURED GREEN on a tray (#491) after the #490 fix, over BOTH planes, adjudicated by t1_22_video_declaration_arms.py whose eighteen self-test controls pin the verdict order and exercise all four verdicts. Seven arms ran: three on the RL plane and four on the SFT plane operators actually train with. The deciding arm declares 'video' through FOUNDATIONSCALE_TRAIN_VIDEO_COLUMN and refuses 96 naming the modality, the variable that declared it and the way out; and its refusal is NOT the missing-text message a corpus with no text column earns. That distinction is the whole row: both refusals exit 96, so the measurer discriminates them with a regex anchored on the real wording in src/foundationscale/train/loop.py rather than on the fixture, and a self-test control plants each wording and requires the flag to set for one and not the other. The undeclared control carries a mere 'video' key alongside a usable text column, TRAINS (2 examples tokenized) and prints a [fs:train:data] notice naming the dropped column -- refusing that one would be name-sniffing, so a control scores it RED if it refuses, and that is what keeps opting out free. An image control declaring a column the corpus lacks still refuses 96, so the plane's ability to refuse a declaration at all is measured rather than assumed. Before #490 this row was MEASURED RED (#477): the same deciding corpus exited 0, tokenized 2 examples, and the word appeared neither in its output nor in 30,671 characters of run manifest -- the declaration was dropped in silence and the run looked healthy. The RL plane refused video by name throughout, before any model is consulted, with a written rationale about frame budgets; what #490 changed is that the training entry point can now reach a refusal of its own. #506 (found by running this row's own undeclared control against a REAL omni corpus, then fixed): the dropped-column notice quoted above can only speak while the modality still has a COLUMN to name, and the estate's 210-row assembly-video corpus leaves none to name. A corpus that KEEPS a separate media column still trips that notice -- that is precisely this row's undeclared control, and it is why the notice looked sufficient. But an omni corpus is under no obligation to keep one: the thin path requires a 'text' column and refuses a corpus without it, and the conversion that satisfies that requirement folds the media reference into the text. Once folded there is no column left to name, and the notice falls silent on the shape the conversion produces. Measured on a tray BEFORE the fix: 210/210 rows retained a '<video>' marker, that checkpoint's own tokenizer split the marker into three ORDINARY tokens (ids [236820, 10540, 236813] = ['<','video','>'], none of them a special id), so it carries none of the unmasked-placeholder loss signature that made #450 visible, twenty steps ran, the save gates passed and the process exited 0 without a single line naming the markers -- the only occurrences of 'video' anywhere in that console were the dataset path, the output directory and the driver's own echo, none of which is a disclosure. The model-side twin already spoke -- modality.dormant_towers reported vision_tower and audio_tower carried and not trained -- so the run disclosed that the MODEL's video-capable towers went untrained here, while staying silent about the DATA assuming they had not. AFTER the fix the same arm prints '[fs:train:data] text-only arm: supervision retains unresolved modality placeholders (<video> in 210/210 rows)' and still exits 0: disclosure, not refusal, because the fold is legitimate and '<video>' can be ordinary prose -- refusing it would refuse the conversion this plane's own refusal text recommends ('Unset FOUNDATIONSCALE_TRAIN_VIDEO_COLUMN to train text-only on the same corpus'). The claim above is unchanged and still GREEN: declaring video still refuses cleanly. What changed is that NOT declaring it is no longer silent. VERIFIED AFTER THE FIX on the same tray, same corpus, same 20 steps: the notice fires twice (once per rank) reading '<video> in 210/210 rows', the UNMEASURED fallback does not fire (the scan really happened -- datasets hands back a Column, a Sequence-registered lazy view and NOT a list, so the accessor's ABC test is load-bearing), the save gate still reports PASS 4/4 (2 verified, 2 declared SKIP) and the process still exits 0. The fix is a disclosure, not a refusal, and the run it describes is the run that previously said nothing. The model-side twin printed alongside it reads 'vision_tower, audio_tower present on the checkpoint but not exercised by this run's declaration ... CARRIED, not trained' -- which is the claim this row now makes, and is weaker than the 'the model cannot see video' framing an earlier draft of this status asserted and a hostile audit of my own prose correctly refused. Recording the fix's own cost: writing that verification down BROKE the verifier (#507). derive_countables classified a row by containment -- 'MEASURED' present and 'UNMEASURED' absent -- so this row measuring on a tray and then reporting, four thousand characters later, that an UNMEASURED fallback did NOT fire was struck from the measured tally for mentioning the token. Containment cannot separate use from mention. C4 duly reported doc and matrix disagreeing, and the cheapest way to make them agree was to edit the honest count down to 19 -- a gate whose easiest remedy is to publish a false number is aimed at the wrong file. Resolved by POSITION instead (first verdict token wins; a leading-token rule would have been wrong too, since T1-16 states its verdict 152 characters in), with a fourth SC9 case that the previous implementation fails and the previous three cases could not see. The IMAGE arm's scan was added without hardware behind it -- unit tests and an AST pin, nothing more -- so it was measured separately on the same tray before being claimed. A 120-row omni probe was built from the estate's decode-filtered image corpus with a '<video>' reference folded into 47 of the rows, which is the shape that arm exists for: pixels present AND a media reference the run will not load. Result: '<video> in 47/120 rows' (the true fraction, not a boolean wearing a fraction's coat), '<image>' correctly NOT reported because the collator loads pixels for it, the announced-abstention branch did not fire, precision PASS over {'BF16': 2076}, PASS 4/4 at checkpoint-20, rc=0. The negative control on the declaration axis therefore holds on hardware and not only in a fixture, which matters here more than the positive result does: a notice that named a modality the run DID load would be false, and false disclosures train operators to skip true ones. |
| T1-23 | declaring audio REFUSES cleanly today | audio corpus; and after #490, four arms on a tray -- audio-only, an undeclared text+audio record, an image control, and the deciding declared-audio arm | must be 96, not a silent drop of the audio field, and the 96 must name the MODALITY rather than the missing text -- a corpus holding nothing but a timestamp earns the latter identically; the undeclared arm must TRAIN with its drop NAMED and scores RED if it refuses; and #454/#463/#476 stand as the pre-fix must-fire, where the deciding arm exited 0 and the audio-only arm's 96 was about the absent text column | ~2 min on 1 GPU for the four arms; each arm must see exactly ONE device -- with two visible, this single-process run wraps in DataParallel and wedges silently (#492) | MEASURED GREEN on a tray (#491) after the #490 fix, four arms of the real training entry point on a small text model, adjudicator t1_23_audio_declaration_arms.py, 29 of 29 self-test controls passing under a bare interpreter. The deciding arm declares 'audio' through FOUNDATIONSCALE_TRAIN_AUDIO_COLUMN and exits 96 saying "audio column 'audio' is declared via FOUNDATIONSCALE_TRAIN_AUDIO_COLUMN, but this training plane has no audio arm: it encodes text, and pixels when an image column is declared", and the manifest it writes records stage=refused together with the declared audio column -- the word now appears in the manifest that previously carried zero occurrences of it. The undeclared control puts text and audio in the same record and TRAINS: exit 0, 2 examples tokenized, with "[fs:train:data] text-only arm: tokenizing 'text'; columns ['audio'] are dropped and contribute nothing to the loss". The drop stays and the silence goes, which is the narrow change this row needed -- refusing a corpus for merely carrying a key named audio would be name-sniffing, and a control scores that RED. A corpus whose only field is audio still exits 96 for the missing text column, correctly, and an image control declaring a column the corpus lacks exits 96 naming that column, so the instrument is shown able to see a refusal rather than assumed to be. Before #490 this row was MEASURED RED (#454, #463, #476): the deciding arm exited 0 with the word appearing nowhere in 20,353 characters of run manifest, and the audio-only arm's 96 read "the thin path requires a 'text' column" -- a refusal about the absent text that a corpus carrying nothing but a timestamp earns identically, so the right exit code stood for the wrong reason. The instrument's own fingerprint is still ruled out rather than assumed: every arm asserts that no element of its argv and no path it writes carries the substring, so a scan cannot match the harness's words in place of the framework's. |

## Rows that cannot fail

The control-arm rule: a row with no arm that must come out differently is a row that cannot fail, and a row that cannot fail is not a measurement. This campaign has now produced several of those — #291, #294, and #372 — which is why every row in this matrix carries an explicit control arm, and why the JSON rendering of this matrix enforces, as a gate, that no `control_arm` field is empty.

## Ordering rule

T0-9 has left the queue, and it is the one row here that this matrix's own harness found RED and then saw fixed. The recording half was never wrong: an unversioned tree is recorded as such, with no commit invented for it, and a real checkout records the commit an independently read HEAD reports — so no row below ever inherited a misattribution. The loudness half was wrong, and measurably so: nothing outside the provenance module read code.status, so a run unpacked from a tarball exited clean without one of its lines saying it could not be attributed. It says so now, and a control run from a real checkout stays quiet, which is the part that keeps the line worth reading. T0-1..T0-8 are done: #373 shipped them, and their CPU-only proofs now run in the suite on every commit, so they no longer sit in this queue. The queue is now empty, which is a statement about scheduling and not about greenness: T1-3, T1-22, T1-23, T1-4 and T1-1 have each been taken and each reports a measurement, T1-16 has had both its runtime arm and, later, its declared control arm run and now reports a verdict on the half of its claim metadata can reach and a blocking abstention on the half it cannot, and T1-5 was never schedulable at all -- the capability it names is unimplemented, so there is nothing to run it against until a resume path exists. T1-21 has left the queue: the arm that #448 found unreachable on the Gemma-4 family is reachable now, and the row's tower-movement claim was measured on a tray against its own declared control -- the image arm moved every vision-tower module and the text-only arm moved none of them while still training its language anchor, so the null result is a measurement and not an arm that failed to run. T1-11 has left the queue: it ran on a tray and came back GREEN over three interleaved rounds, and the #408 false RED it was carrying did not reproduce — which is the useful part, because #408 was a harness defect and the only thing that can retire a harness defect is the harness being run again. T1-6 has left the queue — #343 shipped it — and it is worth saying why it was cheap, because the reason is not that the claim was small. The whole defect is the ORDER of two stdlib calls, so proving it needs one real checkpoint and no tray: only shard headers are read, and a fifty-gigabyte checkpoint costs the same as a fifty-kilobyte one. T1-14 has left the queue with T1-13: the plane refuses both run arms, so there is nothing to schedule until a ZeRO or offload backend exists to schedule it against.

## Running the matrix, and how far the runner reaches

`validation_campaigns/verification_matrix/run_row.py` is what makes this ledger executable rather than descriptive: it drives a row's adjudicator, composes the argv from nothing but the flags that adjudicator's own `--help` declares, and writes a receipt that records the adjudicator's sha256, the claim's sha256, the interpreter and the platform — never a result back into `matrix.json`, because a runner that can edit its own ledger is not evidence. It was driven end-to-end on a tray for the first time at this commit, and two rows came back GREEN through it with receipts: the optimizer row over four arms and the accumulation row over two.

That pass also measured the runner itself, which is the part worth writing down. Of the ten rows that shipped an adjudicator when this was written, the runner could drive six. The other four — the scaler row, the save-load parity row, the symlinked-shard row and the audio row — declared no `--out-dir`, so there was nowhere to hand them an output directory and the runner refused them REFUSE rather than pretending. Three of those four have since gained one and the population has grown, so the number is re-measured at this commit by the runner's own criterion -- asking each adjudicator for the flags its own `--help` declares, which is what the runner composes argv from: twenty-two rows now declare an adjudicator, ten of them declare the `--out-dir` the runner must supply, including the scaler, save-load parity and symlinked-shard rows, the last of those by #494, and twelve still do not, the audio row among them. Declaring the flag is necessary and not sufficient -- the vision-tower row declares it and still refuses on its own terms, for the reason given below -- so this counts instruments the runner can address, not measurements it can carry. That is the honest code: the refusal names itself a defect in the instrument rather than an unmeasured property of the claim, and it is never a red. But it means the ledger's coverage and the runner's coverage are not the same number, and until those four adjudicators grow a run mode, "every adjudicated row" and "every row the runner can take" describe different sets (#472). The rows themselves are not unmeasured — the scaler, parity, symlink and audio rows were all measured by hand — so what is missing is reproducibility through one entry point, not evidence.

Wiring the text-movement row into the runner then exposed a second defect in the instrument, and this one was worse than a refusal because it was silent (#473). `--flag` collected its values into a dict keyed by the flag's name, so asking for `--flag adapter-target=q_proj --flag adapter-target=v_proj` composed `--adapter-target v_proj` alone: the first value was overwritten and nothing said so. Any adjudicator flag declared `action="append"` could therefore never receive more than one value through the harness, and the harness did not refuse — it ran a narrower experiment than the operator asked for and wrote a receipt that looked clean. The text-movement row adapts both `q_proj` and `v_proj`, so the first harness-driven pass of that row would have measured half the claim while reporting the whole one. The fix accumulates instead of overwriting and emits one flag occurrence per value; a single value composes the identical argv it always did, which is the part that has a control of its own. Both halves of the path are controlled separately, and reverting either one alone makes the self-test fire.

Running the row again with the flag fixed then found a third defect, and it is the most instructive of the three because every part of it worked (#474). The training ran, both arms completed, the adjudicator read the adapters and exited 0 — a real GREEN on real hardware — and the harness turned it into REFUSE 96 with the reason "exited 0 but wrote no arm files". The runner reads its observations from `<out-dir>/<row>_<arm>.json` and never from stdout, because a number that existed only in a scrollback is the thing it was built to stop; this adjudicator printed its payload to stdout and wrote only the per-arm training directories. So the seam discarded the measurement. That refusal is the runner behaving correctly — it saw no observations and declined to invent a verdict from prose — and it is also the whole cost of an undeclared convention: the adjudicator was written before it was wired to the runner, nothing in the repo checked that the two agreed, and the disagreement was invisible until a row was driven end to end. The adjudicator now writes an arm payload per arm, including for an arm that failed, since an absent file cannot be told apart from an arm that never ran. The control for it loads the shipped `run_row.py` and calls the shipped `collect_arms()` over a directory the adjudicator itself wrote, rather than re-stating the naming rule in a second place where it could drift again — renaming the payload makes it fire.

With all three fixed, the row was taken through the harness on a tray and came back GREEN with a receipt: `q_proj` and `v_proj` both adapted, the run arm moving 56 of 56 `lora_B` tensors with Frobenius norms from 0.008017275482416153 to 0.020371997728943825, the null arm leaving all 56 at a maximum of exactly 0.0, under Python 3.11.15 on aarch64. Those numbers reproduce the ones taken by hand, by a different route, with the adjudicator's own sha256 in the receipt. The general lesson is the one worth keeping: all three defects were in the instrument rather than in any claim, none of them was visible from reading the code, and each surfaced the first time something was actually run through it end to end.

Asking how far that lesson generalised found a fourth defect in the runner itself, and it is the same trap one level down (#475). Rule 5 refuses an exit of 0 or 5 with ZERO arm files, because a verdict with no observations behind it is `all([])`. It did not refuse an arm file that parsed, was an object, and could not say whether the arm had been measured. In the limit such a payload lifts nothing at all and the receipt reads `arms: {"run": {}}` beside `verdict: green` — an empty MEMBER where rule 5 only ever guarded against an empty SET. The two real payloads that hit this are less stark than that and worse for it: both carry evidence — the accumulation row's arms lift telemetry and a loss curve, the gradient-checkpointing row's lift telemetry, excerpts and a launcher exit code — so the arm entries are populated and the receipt reads as substantiated, while neither arm anywhere states that it was measured. An empty entry would at least have looked wrong. Reverting the fix makes the runner exit 0 on exactly that input and print "claim upheld; 1 arm(s) measured", which is the receipt asserting a measurement that is not in it. Only `status` is required and the other five keys stay optional on purpose: the evidence keys are genuinely per-row — a row whose evidence is adapter norms has no loss curve — whereas `status` is not evidence but the arm's identity, the one thing a receipt cannot be silent about. The control writes a payload that DOES carry telemetry, so a weaker fix that merely demanded a non-empty dict would not satisfy it.

That defect was found by reviewing a second adjudicator rather than by running one, and the review raised a question nobody had asked: how many of the rows that declare an adjudicator can actually carry a measurement through the seam today? The first answer was a reading of the source, and it was wrong. It counted the adjudicators that write per-arm payload files by pattern-matching how each one builds a path, concluded four, and predicted that the gradient-checkpointing row wrote none; driven through the runner, that row exited 0 and wrote nine. A pattern that can misread one row can misread any of them, so the remaining rows were run rather than read, and all ten now have a measured answer instead of an inferred one (#476).

Five carry a payload through the seam — the optimizer, accumulation, gradient-checkpointing, attention-backend and text-movement rows — writing four, two, nine, fourteen and two arm files. Four refuse before execution for the #472 reason above, declaring no `--out-dir` and so having nowhere to be handed one; that count, and every per-payload count in this paragraph and the next, was taken at #476 over the ten-row population of the time, and re-taking it needs the rows run end to end rather than re-read, so they are recorded here as the reading they were and not as the state today. The tenth, the vision-tower row, is reachable and refuses on its own terms: the model, the two corpora and the two module patterns it needs are estate facts with no defensible default, so it declines to invent them. That last outcome is the adjudicator working correctly rather than a defect, and the earlier reading had no way to express it — which is the particular way a count of files misleads, because it cannot tell an instrument that cannot be reached apart from one that reaches the claim and honestly declines.

Of the five that do carry payloads, three say whether their arms were measured and two do not, so #475 converts two standing harness greens into refusals: the accumulation row and the gradient-checkpointing row both exited 0, and neither receipt could substantiate it. The hand-taken measurements behind those two rows are untouched and still stand; what no longer stands is the claim that the harness reproduced them. The cause is not carelessness in two files but a contract that was never written down. The five payload writers agree on almost nothing: three write `status`, one writes `refused` in its place and one writes neither; three write `launcher_exit_code`, one writes `launcher_exit` and one writes no exit key at all; three write `loss_curve`, one writes `loss_series` and one writes neither. `ARM_SCALAR_KEYS` was a list of keys the runner happens to lift, never a contract published to anyone writing a payload, so each adjudicator invented its own spelling and none of them was wrong to. The fix that matches that cause is a shared writer exported beside `collect_arms()` so the naming rule lives in one place; widening the lift list to accept each new spelling would bless the divergence and guarantee a sixth. The gap remains the number that matters: the matrix declared an adjudicator for ten rows when that was measured, and the shipped harness could then carry a substantiated receipt from three of them — two green and one an honest UNMEASURED. That gap is owed a fresh reading rather than a re-derivation from this one, because the adjudicator population has since grown and #493 has since named a third spelling divergence in the lift.

## The honest count
36 rows: 13 that need no GPU and 23 that do. Of those, 0 now name an axis the training plane does not expose at all, and 0 are blocked by another row, so 0 are unrunnable for want of a declaration — the lower tier has shipped, and for most of the remaining rows what stands between this matrix and a reading is a tray, not a missing knob. For the resume row and the rows marked ABSENT it is neither: the capability itself is unimplemented, which the unrunnable countable does not capture because it counts rows lacking a declaration, not rows lacking an implementation. 23 have produced a measurement, and they are not in the same state as each other: the optimizer row, the gradient-accumulation row, the gradient-checkpointing row, the vision-tower row, the save-load parity row, the half-precision grad-scaler row and the four-bit precision-refusal row were taken on a tray under the manifest that records them and came out GREEN, the symlinked-shard row came out GREEN too but needed no tray -- it reads shard headers and never tensor data, so a real twelve-shard checkpoint of roughly fifty-five gigabytes costs it what a small one does, the optimizer-offload row, the sharded-execution row and the expert-parallel row were measured together on a tray and came out GREEN on a narrower question than their own claims ask -- each proves that declaring the axis is refused at that axis's own refusal site, before the model loads and with no weights left behind, rather than silently trained as plain data parallelism under a manifest that says otherwise, and none of them shows the capability itself, which this plane does not have, the package-API adapter row came out GREEN and in doing so retracted its own previous entry, which had recorded the capability as absent on the strength of a reading of the source rather than of a run -- a config built by hand, with no command line anywhere, attaches the adapter, trains it, leaves the base frozen and records the declaration in the manifest, and the full-fine-tune control moves base weights so that the frozen reading is a reading and not an absence of instrumentation, and the tied-embedding row came out green on the half of its claim this plane can reach, retracting a second entry that had recorded a capability as absent on the strength of a reading rather than a run -- a trained tie survives the save and load round trip with the output head still sharing the input embedding's storage, while an untied control built from the same config, differing in exactly one field, reloads with separate storage and so shows the detector can report a mismatch, and the sharding half of that row stays unreachable because this plane refuses sharded execution, and the checkpoint-parity row came out green on verification, retracting a third entry that had recorded a capability as absent on the strength of a reading rather than a run -- the framework reads a distributed-checkpoint directory and a safetensors checkpoint through one interface and grades them on three levels rather than two, so a single element moved by one unit in the last place is reported as seen and close rather than silently absorbed into a pass, the same element moved past the declared tolerance blocks and names the key, a dropped tensor fails as a one-sided key rather than as a numeric difference, and comparing two checkpoints that share no keys at all reports itself vacuous rather than agreeing; what that row does not show, and no longer claims, is that anything here CONVERTS between the two formats, because nothing does, the declared-audio and declared-video rows were taken the same way and each came out RED, and each has since been re-measured GREEN after the declaration boundary was fixed -- the plane had refused a declared image column it could not honour and dropped a declared sound column without a word, and video was already refused deliberately and by name in the reinforcement-learning plane while the training entry point could not reach that refusal, so the same declaration that halted one plane was dropped in silence by the other; the fix carried both declarations to the same refusal site, and the arms that decide those rows now run over both planes, the mixture-of-experts row was taken on a tray and is neither — a real sparse checkpoint trained end-to-end and the run returned clean, and the gates that adjudicate the half of the claim anyone cares about no longer skip themselves, because the reader that had declared a model with one hundred and twenty-eight experts to be dense was consulting a flat top-level field while every such model on this estate nests the count one level down, which is fixed and re-measured; what that row still lacks is not a count but an instrument, since the dominant layout packs every expert of a layer into a single tensor and a per-key comparison cannot see one expert inside it, so the row now has a run, a verdict on the half that metadata can reach, and on the half it cannot an abstention that blocks rather than a pass, while the rows that predated that manifest have since been re-taken and the control arm that had never run has now run, so what stood as a reading is a differential. 0 remain as asserted refusals: the last of them, the four-bit precision row, has now been measured on a tray -- it exits ninety-six from the training loop's own refusal site, names the absent backend, trains nothing and writes no checkpoint, while an otherwise identical half-precision arm trains to completion and writes its weights on the same fixtures, so the emptiness beside the refusal is one that has been shown it could have been filled.

The row that arrives in the measured bucket this round is the per-layer attention span, and it is worth saying what kind of measurement it is. The FLOP formula had assumed every layer attends over the whole sequence, which is false for both hybrid and sliding-window families, so the quadratic term was charged too large -- an error that understates this framework's own reported efficiency rather than flattering it. The correction is arithmetic checked against real declared layouts on a laptop, not a reading taken on hardware, and the row says so: what it settles is that the formula now charges the span each layer actually attends over, and what it does not settle is any end-to-end utilisation figure, which still waits on a tray and on a declared device peak. The asymmetry with the expert row beside it is deliberate rather than inconsistent -- an unresolvable expert count overstates by a multiple and must abstain, while this term is a few percent and errs downward, so it falls back and declares the fallback instead of taking the metric offline for every hybrid model.

The expert-parallel row has moved into the measured bucket above, and on direct evidence rather than on the topology legs alone: declaring two-way expert parallelism is refused at the unwired-degree site, the message names the degree it refused, the refusal precedes the model load and leaves no weights, and the same flag at its honourable value trains to completion on identical fixtures. The axis is still not wired, and the row is still not a demonstration of expert parallelism. What changed is that the plane's saying so is now a reading rather than a reading of the source.

Every number in that paragraph is derived from `matrix.json` by `checks/verification_matrix.py`, which reads them back positionally and refuses a row whose control arm is a placeholder. They are not restated by hand, and the table above is a rendering of the same ledger. The two tiers are described in words here rather than by their numeric labels because the countables check reads every integer in this section positionally, so a digit inside a label would itself be adjudicated as a countable.
